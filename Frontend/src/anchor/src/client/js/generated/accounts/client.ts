/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "gill";
import {
  getValidatedCallbackDecoder,
  getValidatedCallbackEncoder,
  type ValidatedCallback,
  type ValidatedCallbackArgs,
} from "../types";

export const CLIENT_DISCRIMINATOR = new Uint8Array([
  221, 237, 145, 143, 170, 194, 133, 115,
]);

export function getClientDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLIENT_DISCRIMINATOR);
}

export type Client = {
  discriminator: ReadonlyUint8Array;
  bump: number;
  /**
   * The owner is able to manage the client:
   *
   * -   withdraw client funds
   * -   transfer ownership
   */
  owner: Address;
  /** An address of a registered program. */
  program: Address;
  /**
   * An arbitrary PDA that belongs to the client program.
   *
   * This is the request authority.
   */
  state: Address;
  /** Number of requests made by the client. */
  numRequests: bigint;
  /**
   * An optional client-level callback.
   *
   * **Note:** this callback does not apply to [`RequestAlt`].
   *
   * If it is `None`, then no callback will be called upon request fulfill, but you can
   * override this using the request-level callback (see [`RequestParams::callback`]).
   *
   * You can update this value using the [`SetCallback`] instruction.
   *
   * [`RequestParams::callback`]: crate::RequestParams::callback
   * [`SetCallback`]: crate::SetCallback
   * [`RequestAlt`]: crate::RequestAlt
   */
  callback: Option<ValidatedCallback>;
};

export type ClientArgs = {
  bump: number;
  /**
   * The owner is able to manage the client:
   *
   * -   withdraw client funds
   * -   transfer ownership
   */
  owner: Address;
  /** An address of a registered program. */
  program: Address;
  /**
   * An arbitrary PDA that belongs to the client program.
   *
   * This is the request authority.
   */
  state: Address;
  /** Number of requests made by the client. */
  numRequests: number | bigint;
  /**
   * An optional client-level callback.
   *
   * **Note:** this callback does not apply to [`RequestAlt`].
   *
   * If it is `None`, then no callback will be called upon request fulfill, but you can
   * override this using the request-level callback (see [`RequestParams::callback`]).
   *
   * You can update this value using the [`SetCallback`] instruction.
   *
   * [`RequestParams::callback`]: crate::RequestParams::callback
   * [`SetCallback`]: crate::SetCallback
   * [`RequestAlt`]: crate::RequestAlt
   */
  callback: OptionOrNullable<ValidatedCallbackArgs>;
};

/** Gets the encoder for {@link ClientArgs} account data. */
export function getClientEncoder(): Encoder<ClientArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["bump", getU8Encoder()],
      ["owner", getAddressEncoder()],
      ["program", getAddressEncoder()],
      ["state", getAddressEncoder()],
      ["numRequests", getU64Encoder()],
      ["callback", getOptionEncoder(getValidatedCallbackEncoder())],
    ]),
    (value) => ({ ...value, discriminator: CLIENT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Client} account data. */
export function getClientDecoder(): Decoder<Client> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["bump", getU8Decoder()],
    ["owner", getAddressDecoder()],
    ["program", getAddressDecoder()],
    ["state", getAddressDecoder()],
    ["numRequests", getU64Decoder()],
    ["callback", getOptionDecoder(getValidatedCallbackDecoder())],
  ]);
}

/** Gets the codec for {@link Client} account data. */
export function getClientCodec(): Codec<ClientArgs, Client> {
  return combineCodec(getClientEncoder(), getClientDecoder());
}

export function decodeClient<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Client, TAddress>;
export function decodeClient<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Client, TAddress>;
export function decodeClient<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Client, TAddress> | MaybeAccount<Client, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getClientDecoder(),
  );
}

export async function fetchClient<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Client, TAddress>> {
  const maybeAccount = await fetchMaybeClient(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeClient<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Client, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeClient(maybeAccount);
}

export async function fetchAllClient(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Client>[]> {
  const maybeAccounts = await fetchAllMaybeClient(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeClient(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Client>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeClient(maybeAccount));
}
