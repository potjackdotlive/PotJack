/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "gill";
import {
  getNetworkConfigurationDecoder,
  getNetworkConfigurationEncoder,
  type NetworkConfiguration,
  type NetworkConfigurationArgs,
} from "../types";

export const NETWORK_STATE_DISCRIMINATOR = new Uint8Array([
  212, 237, 148, 56, 97, 245, 51, 169,
]);

export function getNetworkStateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    NETWORK_STATE_DISCRIMINATOR,
  );
}

export type NetworkState = {
  discriminator: ReadonlyUint8Array;
  /** Account bump. */
  bump: number;
  /** Active configuration. */
  config: NetworkConfiguration;
  /** Total number of received requests. */
  numRequests: bigint;
  /** Total number of registered clients. */
  numRegistered: bigint;
  /** Total number of terminated clients. */
  numTerminated: bigint;
};

export type NetworkStateArgs = {
  /** Account bump. */
  bump: number;
  /** Active configuration. */
  config: NetworkConfigurationArgs;
  /** Total number of received requests. */
  numRequests: number | bigint;
  /** Total number of registered clients. */
  numRegistered: number | bigint;
  /** Total number of terminated clients. */
  numTerminated: number | bigint;
};

/** Gets the encoder for {@link NetworkStateArgs} account data. */
export function getNetworkStateEncoder(): Encoder<NetworkStateArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["bump", getU8Encoder()],
      ["config", getNetworkConfigurationEncoder()],
      ["numRequests", getU64Encoder()],
      ["numRegistered", getU64Encoder()],
      ["numTerminated", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: NETWORK_STATE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link NetworkState} account data. */
export function getNetworkStateDecoder(): Decoder<NetworkState> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["bump", getU8Decoder()],
    ["config", getNetworkConfigurationDecoder()],
    ["numRequests", getU64Decoder()],
    ["numRegistered", getU64Decoder()],
    ["numTerminated", getU64Decoder()],
  ]);
}

/** Gets the codec for {@link NetworkState} account data. */
export function getNetworkStateCodec(): Codec<NetworkStateArgs, NetworkState> {
  return combineCodec(getNetworkStateEncoder(), getNetworkStateDecoder());
}

export function decodeNetworkState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<NetworkState, TAddress>;
export function decodeNetworkState<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<NetworkState, TAddress>;
export function decodeNetworkState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<NetworkState, TAddress> | MaybeAccount<NetworkState, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getNetworkStateDecoder(),
  );
}

export async function fetchNetworkState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<NetworkState, TAddress>> {
  const maybeAccount = await fetchMaybeNetworkState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeNetworkState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<NetworkState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeNetworkState(maybeAccount);
}

export async function fetchAllNetworkState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<NetworkState>[]> {
  const maybeAccounts = await fetchAllMaybeNetworkState(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeNetworkState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<NetworkState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeNetworkState(maybeAccount));
}
