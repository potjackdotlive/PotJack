/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "gill";
import {
  getRequestStateDecoder,
  getRequestStateEncoder,
  type RequestState,
  type RequestStateArgs,
} from "../types";

export const REQUEST_ACCOUNT_DISCRIMINATOR = new Uint8Array([
  108, 23, 6, 158, 184, 6, 152, 121,
]);

export function getRequestAccountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REQUEST_ACCOUNT_DISCRIMINATOR,
  );
}

export type RequestAccount = {
  discriminator: ReadonlyUint8Array;
  /** PDA bump. */
  bump: number;
  /** The slot this request was created at. */
  slot: bigint;
  /** The client created the request. */
  client: Address;
  /** Request seed. */
  seed: ReadonlyUint8Array;
  /** The state of this randomness request. */
  state: RequestState;
};

export type RequestAccountArgs = {
  /** PDA bump. */
  bump: number;
  /** The slot this request was created at. */
  slot: number | bigint;
  /** The client created the request. */
  client: Address;
  /** Request seed. */
  seed: ReadonlyUint8Array;
  /** The state of this randomness request. */
  state: RequestStateArgs;
};

/** Gets the encoder for {@link RequestAccountArgs} account data. */
export function getRequestAccountEncoder(): Encoder<RequestAccountArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["bump", getU8Encoder()],
      ["slot", getU64Encoder()],
      ["client", getAddressEncoder()],
      ["seed", fixEncoderSize(getBytesEncoder(), 32)],
      ["state", getRequestStateEncoder()],
    ]),
    (value) => ({ ...value, discriminator: REQUEST_ACCOUNT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link RequestAccount} account data. */
export function getRequestAccountDecoder(): Decoder<RequestAccount> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["bump", getU8Decoder()],
    ["slot", getU64Decoder()],
    ["client", getAddressDecoder()],
    ["seed", fixDecoderSize(getBytesDecoder(), 32)],
    ["state", getRequestStateDecoder()],
  ]);
}

/** Gets the codec for {@link RequestAccount} account data. */
export function getRequestAccountCodec(): Codec<
  RequestAccountArgs,
  RequestAccount
> {
  return combineCodec(getRequestAccountEncoder(), getRequestAccountDecoder());
}

export function decodeRequestAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<RequestAccount, TAddress>;
export function decodeRequestAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<RequestAccount, TAddress>;
export function decodeRequestAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<RequestAccount, TAddress> | MaybeAccount<RequestAccount, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getRequestAccountDecoder(),
  );
}

export async function fetchRequestAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<RequestAccount, TAddress>> {
  const maybeAccount = await fetchMaybeRequestAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeRequestAccount<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<RequestAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeRequestAccount(maybeAccount);
}

export async function fetchAllRequestAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<RequestAccount>[]> {
  const maybeAccounts = await fetchAllMaybeRequestAccount(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeRequestAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<RequestAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeRequestAccount(maybeAccount),
  );
}
