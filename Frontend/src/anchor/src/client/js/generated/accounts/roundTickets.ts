/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "gill";

export const ROUND_TICKETS_DISCRIMINATOR = new Uint8Array([
  100, 189, 20, 223, 39, 44, 161, 211,
]);

export function getRoundTicketsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ROUND_TICKETS_DISCRIMINATOR,
  );
}

export type RoundTickets = {
  discriminator: ReadonlyUint8Array;
  round: Address;
  cumulativeTickets1: Array<number>;
  cumulativeTickets2: Array<number>;
  len: number;
  bump: number;
  padding: ReadonlyUint8Array;
};

export type RoundTicketsArgs = {
  round: Address;
  cumulativeTickets1: Array<number>;
  cumulativeTickets2: Array<number>;
  len: number;
  bump: number;
  padding: ReadonlyUint8Array;
};

/** Gets the encoder for {@link RoundTicketsArgs} account data. */
export function getRoundTicketsEncoder(): FixedSizeEncoder<RoundTicketsArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["round", getAddressEncoder()],
      ["cumulativeTickets1", getArrayEncoder(getU32Encoder(), { size: 1024 })],
      ["cumulativeTickets2", getArrayEncoder(getU32Encoder(), { size: 1024 })],
      ["len", getU32Encoder()],
      ["bump", getU8Encoder()],
      ["padding", fixEncoderSize(getBytesEncoder(), 3)],
    ]),
    (value) => ({ ...value, discriminator: ROUND_TICKETS_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link RoundTickets} account data. */
export function getRoundTicketsDecoder(): FixedSizeDecoder<RoundTickets> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["round", getAddressDecoder()],
    ["cumulativeTickets1", getArrayDecoder(getU32Decoder(), { size: 1024 })],
    ["cumulativeTickets2", getArrayDecoder(getU32Decoder(), { size: 1024 })],
    ["len", getU32Decoder()],
    ["bump", getU8Decoder()],
    ["padding", fixDecoderSize(getBytesDecoder(), 3)],
  ]);
}

/** Gets the codec for {@link RoundTickets} account data. */
export function getRoundTicketsCodec(): FixedSizeCodec<
  RoundTicketsArgs,
  RoundTickets
> {
  return combineCodec(getRoundTicketsEncoder(), getRoundTicketsDecoder());
}

export function decodeRoundTickets<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<RoundTickets, TAddress>;
export function decodeRoundTickets<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<RoundTickets, TAddress>;
export function decodeRoundTickets<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<RoundTickets, TAddress> | MaybeAccount<RoundTickets, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getRoundTicketsDecoder(),
  );
}

export async function fetchRoundTickets<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<RoundTickets, TAddress>> {
  const maybeAccount = await fetchMaybeRoundTickets(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeRoundTickets<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<RoundTickets, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeRoundTickets(maybeAccount);
}

export async function fetchAllRoundTickets(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<RoundTickets>[]> {
  const maybeAccounts = await fetchAllMaybeRoundTickets(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeRoundTickets(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<RoundTickets>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeRoundTickets(maybeAccount));
}

export function getRoundTicketsSize(): number {
  return 8240;
}
