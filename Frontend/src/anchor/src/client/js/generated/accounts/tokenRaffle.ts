/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "gill";
import {
  getRoundStatusDecoder,
  getRoundStatusEncoder,
  type RoundStatus,
  type RoundStatusArgs,
} from "../types";

export const TOKEN_RAFFLE_DISCRIMINATOR = new Uint8Array([
  10, 32, 165, 142, 80, 191, 11, 4,
]);

export function getTokenRaffleDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TOKEN_RAFFLE_DISCRIMINATOR,
  );
}

export type TokenRaffle = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
  tokenMint: Address;
  currentRoundId: Option<number>;
  currentRoundStatus: RoundStatus;
  currentRoundEndTime: Option<bigint>;
  totalRounds: number;
  pendingRounds: Array<number>;
  bump: number;
};

export type TokenRaffleArgs = {
  authority: Address;
  tokenMint: Address;
  currentRoundId: OptionOrNullable<number>;
  currentRoundStatus: RoundStatusArgs;
  currentRoundEndTime: OptionOrNullable<number | bigint>;
  totalRounds: number;
  pendingRounds: Array<number>;
  bump: number;
};

/** Gets the encoder for {@link TokenRaffleArgs} account data. */
export function getTokenRaffleEncoder(): Encoder<TokenRaffleArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["tokenMint", getAddressEncoder()],
      ["currentRoundId", getOptionEncoder(getU32Encoder())],
      ["currentRoundStatus", getRoundStatusEncoder()],
      ["currentRoundEndTime", getOptionEncoder(getI64Encoder())],
      ["totalRounds", getU32Encoder()],
      ["pendingRounds", getArrayEncoder(getU32Encoder())],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: TOKEN_RAFFLE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link TokenRaffle} account data. */
export function getTokenRaffleDecoder(): Decoder<TokenRaffle> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["tokenMint", getAddressDecoder()],
    ["currentRoundId", getOptionDecoder(getU32Decoder())],
    ["currentRoundStatus", getRoundStatusDecoder()],
    ["currentRoundEndTime", getOptionDecoder(getI64Decoder())],
    ["totalRounds", getU32Decoder()],
    ["pendingRounds", getArrayDecoder(getU32Decoder())],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link TokenRaffle} account data. */
export function getTokenRaffleCodec(): Codec<TokenRaffleArgs, TokenRaffle> {
  return combineCodec(getTokenRaffleEncoder(), getTokenRaffleDecoder());
}

export function decodeTokenRaffle<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<TokenRaffle, TAddress>;
export function decodeTokenRaffle<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<TokenRaffle, TAddress>;
export function decodeTokenRaffle<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<TokenRaffle, TAddress> | MaybeAccount<TokenRaffle, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getTokenRaffleDecoder(),
  );
}

export async function fetchTokenRaffle<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<TokenRaffle, TAddress>> {
  const maybeAccount = await fetchMaybeTokenRaffle(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTokenRaffle<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<TokenRaffle, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTokenRaffle(maybeAccount);
}

export async function fetchAllTokenRaffle(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<TokenRaffle>[]> {
  const maybeAccounts = await fetchAllMaybeTokenRaffle(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTokenRaffle(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<TokenRaffle>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeTokenRaffle(maybeAccount));
}
