/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  combineCodec,
  fixDecoderSize,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  transformEncoder,
  type WritableAccount,
  type WritableSignerAccount,
} from "gill";
import { u32ToLittleEndianBuffer } from "utils/buffer";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { expectAddress, expectSome, getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const BUY_TICKETS_SOL_DISCRIMINATOR = new Uint8Array([164, 132, 202, 239, 186, 235, 124, 176]);

export function getBuyTicketsSolDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BUY_TICKETS_SOL_DISCRIMINATOR);
}

export type BuyTicketsSolInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountRaffleState extends string | AccountMeta<string> = string,
  TAccountSolRaffle extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountRoundTickets extends string | AccountMeta<string> = string,
  TAccountRentVault extends string | AccountMeta<string> = string,
  TAccountRoundTicketsPurchase extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountBtcPriceFeed extends string | AccountMeta<string> = string,
  TAccountSolPriceFeed extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRaffleState extends string ? ReadonlyAccount<TAccountRaffleState> : TAccountRaffleState,
      TAccountSolRaffle extends string ? WritableAccount<TAccountSolRaffle> : TAccountSolRaffle,
      TAccountRound extends string ? WritableAccount<TAccountRound> : TAccountRound,
      TAccountRoundTickets extends string ? WritableAccount<TAccountRoundTickets> : TAccountRoundTickets,
      TAccountRentVault extends string ? WritableAccount<TAccountRentVault> : TAccountRentVault,
      TAccountRoundTicketsPurchase extends string
        ? WritableAccount<TAccountRoundTicketsPurchase>
        : TAccountRoundTicketsPurchase,
      TAccountPlayer extends string
        ? WritableSignerAccount<TAccountPlayer> & AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountVault extends string ? WritableAccount<TAccountVault> : TAccountVault,
      TAccountBtcPriceFeed extends string ? ReadonlyAccount<TAccountBtcPriceFeed> : TAccountBtcPriceFeed,
      TAccountSolPriceFeed extends string ? ReadonlyAccount<TAccountSolPriceFeed> : TAccountSolPriceFeed,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type BuyTicketsSolInstructionData = {
  discriminator: ReadonlyUint8Array;
  roundId: number;
  purchaseIndex: number;
  count: number;
  maxCost: bigint;
};

export type BuyTicketsSolInstructionDataArgs = {
  roundId: number;
  purchaseIndex: number;
  count: number;
  maxCost: number | bigint;
};

export function getBuyTicketsSolInstructionDataEncoder(): FixedSizeEncoder<BuyTicketsSolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["roundId", getU32Encoder()],
      ["purchaseIndex", getU32Encoder()],
      ["count", getU32Encoder()],
      ["maxCost", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BUY_TICKETS_SOL_DISCRIMINATOR }),
  );
}

export function getBuyTicketsSolInstructionDataDecoder(): FixedSizeDecoder<BuyTicketsSolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["roundId", getU32Decoder()],
    ["purchaseIndex", getU32Decoder()],
    ["count", getU32Decoder()],
    ["maxCost", getU64Decoder()],
  ]);
}

export function getBuyTicketsSolInstructionDataCodec(): FixedSizeCodec<
  BuyTicketsSolInstructionDataArgs,
  BuyTicketsSolInstructionData
> {
  return combineCodec(getBuyTicketsSolInstructionDataEncoder(), getBuyTicketsSolInstructionDataDecoder());
}

export type BuyTicketsSolAsyncInput<
  TAccountRaffleState extends string = string,
  TAccountSolRaffle extends string = string,
  TAccountRound extends string = string,
  TAccountRoundTickets extends string = string,
  TAccountRentVault extends string = string,
  TAccountRoundTicketsPurchase extends string = string,
  TAccountPlayer extends string = string,
  TAccountVault extends string = string,
  TAccountBtcPriceFeed extends string = string,
  TAccountSolPriceFeed extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  raffleState?: Address<TAccountRaffleState>;
  solRaffle?: Address<TAccountSolRaffle>;
  round?: Address<TAccountRound>;
  roundTickets?: Address<TAccountRoundTickets>;
  rentVault?: Address<TAccountRentVault>;
  roundTicketsPurchase?: Address<TAccountRoundTicketsPurchase>;
  player: TransactionSigner<TAccountPlayer>;
  vault?: Address<TAccountVault>;
  btcPriceFeed: Address<TAccountBtcPriceFeed>;
  solPriceFeed: Address<TAccountSolPriceFeed>;
  systemProgram?: Address<TAccountSystemProgram>;
  roundId: BuyTicketsSolInstructionDataArgs["roundId"];
  purchaseIndex: BuyTicketsSolInstructionDataArgs["purchaseIndex"];
  count: BuyTicketsSolInstructionDataArgs["count"];
  maxCost: BuyTicketsSolInstructionDataArgs["maxCost"];
};

export async function getBuyTicketsSolInstructionAsync<
  TAccountRaffleState extends string,
  TAccountSolRaffle extends string,
  TAccountRound extends string,
  TAccountRoundTickets extends string,
  TAccountRentVault extends string,
  TAccountRoundTicketsPurchase extends string,
  TAccountPlayer extends string,
  TAccountVault extends string,
  TAccountBtcPriceFeed extends string,
  TAccountSolPriceFeed extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: BuyTicketsSolAsyncInput<
    TAccountRaffleState,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTickets,
    TAccountRentVault,
    TAccountRoundTicketsPurchase,
    TAccountPlayer,
    TAccountVault,
    TAccountBtcPriceFeed,
    TAccountSolPriceFeed,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  BuyTicketsSolInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTickets,
    TAccountRentVault,
    TAccountRoundTicketsPurchase,
    TAccountPlayer,
    TAccountVault,
    TAccountBtcPriceFeed,
    TAccountSolPriceFeed,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: false },
    solRaffle: { value: input.solRaffle ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    roundTickets: { value: input.roundTickets ?? null, isWritable: true },
    rentVault: { value: input.rentVault ?? null, isWritable: true },
    roundTicketsPurchase: {
      value: input.roundTicketsPurchase ?? null,
      isWritable: true,
    },
    player: { value: input.player ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    btcPriceFeed: { value: input.btcPriceFeed ?? null, isWritable: false },
    solPriceFeed: { value: input.solPriceFeed ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.raffleState.value) {
    accounts.raffleState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([114, 97, 102, 102, 108, 101, 95, 115, 116, 97, 116, 101]))],
    });
  }
  if (!accounts.solRaffle.value) {
    accounts.solRaffle.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([115, 111, 108, 95, 114, 97, 102, 102, 108, 101]))],
    });
  }
  if (!accounts.round.value) {
    accounts.round.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([114, 111, 117, 110, 100])),
        getAddressEncoder().encode(expectAddress(accounts.solRaffle.value)),
        u32ToLittleEndianBuffer(args.roundId),
      ],
    });
  }
  if (!accounts.roundTickets.value) {
    accounts.roundTickets.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([114, 111, 117, 110, 100, 95, 116, 105, 99, 107, 101, 116, 115])),
        getAddressEncoder().encode(expectAddress(accounts.round.value)),
      ],
    });
  }
  if (!accounts.rentVault.value) {
    accounts.rentVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([114, 101, 110, 116, 95, 118, 97, 117, 108, 116]))],
    });
  }
  if (!accounts.roundTicketsPurchase.value) {
    accounts.roundTicketsPurchase.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 111, 117, 110, 100, 95, 116, 105, 99, 107, 101, 116, 115, 95, 112, 117, 114, 99, 104, 97, 115, 101,
          ]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.round.value)),
        getU32Encoder().encode(expectSome(args.purchaseIndex)),
      ],
    });
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([115, 111, 108, 95, 118, 97, 117, 108, 116]))],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.raffleState),
      getAccountMeta(accounts.solRaffle),
      getAccountMeta(accounts.round),
      getAccountMeta(accounts.roundTickets),
      getAccountMeta(accounts.rentVault),
      getAccountMeta(accounts.roundTicketsPurchase),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.btcPriceFeed),
      getAccountMeta(accounts.solPriceFeed),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getBuyTicketsSolInstructionDataEncoder().encode(args as BuyTicketsSolInstructionDataArgs),
    programAddress,
  } as BuyTicketsSolInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTickets,
    TAccountRentVault,
    TAccountRoundTicketsPurchase,
    TAccountPlayer,
    TAccountVault,
    TAccountBtcPriceFeed,
    TAccountSolPriceFeed,
    TAccountSystemProgram
  >);
}

export type BuyTicketsSolInput<
  TAccountRaffleState extends string = string,
  TAccountSolRaffle extends string = string,
  TAccountRound extends string = string,
  TAccountRoundTickets extends string = string,
  TAccountRentVault extends string = string,
  TAccountRoundTicketsPurchase extends string = string,
  TAccountPlayer extends string = string,
  TAccountVault extends string = string,
  TAccountBtcPriceFeed extends string = string,
  TAccountSolPriceFeed extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  raffleState: Address<TAccountRaffleState>;
  solRaffle: Address<TAccountSolRaffle>;
  round: Address<TAccountRound>;
  roundTickets: Address<TAccountRoundTickets>;
  rentVault: Address<TAccountRentVault>;
  roundTicketsPurchase: Address<TAccountRoundTicketsPurchase>;
  player: TransactionSigner<TAccountPlayer>;
  vault: Address<TAccountVault>;
  btcPriceFeed: Address<TAccountBtcPriceFeed>;
  solPriceFeed: Address<TAccountSolPriceFeed>;
  systemProgram?: Address<TAccountSystemProgram>;
  roundId: BuyTicketsSolInstructionDataArgs["roundId"];
  purchaseIndex: BuyTicketsSolInstructionDataArgs["purchaseIndex"];
  count: BuyTicketsSolInstructionDataArgs["count"];
  maxCost: BuyTicketsSolInstructionDataArgs["maxCost"];
};

export function getBuyTicketsSolInstruction<
  TAccountRaffleState extends string,
  TAccountSolRaffle extends string,
  TAccountRound extends string,
  TAccountRoundTickets extends string,
  TAccountRentVault extends string,
  TAccountRoundTicketsPurchase extends string,
  TAccountPlayer extends string,
  TAccountVault extends string,
  TAccountBtcPriceFeed extends string,
  TAccountSolPriceFeed extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: BuyTicketsSolInput<
    TAccountRaffleState,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTickets,
    TAccountRentVault,
    TAccountRoundTicketsPurchase,
    TAccountPlayer,
    TAccountVault,
    TAccountBtcPriceFeed,
    TAccountSolPriceFeed,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): BuyTicketsSolInstruction<
  TProgramAddress,
  TAccountRaffleState,
  TAccountSolRaffle,
  TAccountRound,
  TAccountRoundTickets,
  TAccountRentVault,
  TAccountRoundTicketsPurchase,
  TAccountPlayer,
  TAccountVault,
  TAccountBtcPriceFeed,
  TAccountSolPriceFeed,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: false },
    solRaffle: { value: input.solRaffle ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    roundTickets: { value: input.roundTickets ?? null, isWritable: true },
    rentVault: { value: input.rentVault ?? null, isWritable: true },
    roundTicketsPurchase: {
      value: input.roundTicketsPurchase ?? null,
      isWritable: true,
    },
    player: { value: input.player ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    btcPriceFeed: { value: input.btcPriceFeed ?? null, isWritable: false },
    solPriceFeed: { value: input.solPriceFeed ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.raffleState),
      getAccountMeta(accounts.solRaffle),
      getAccountMeta(accounts.round),
      getAccountMeta(accounts.roundTickets),
      getAccountMeta(accounts.rentVault),
      getAccountMeta(accounts.roundTicketsPurchase),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.btcPriceFeed),
      getAccountMeta(accounts.solPriceFeed),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getBuyTicketsSolInstructionDataEncoder().encode(args as BuyTicketsSolInstructionDataArgs),
    programAddress,
  } as BuyTicketsSolInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTickets,
    TAccountRentVault,
    TAccountRoundTicketsPurchase,
    TAccountPlayer,
    TAccountVault,
    TAccountBtcPriceFeed,
    TAccountSolPriceFeed,
    TAccountSystemProgram
  >);
}

export type ParsedBuyTicketsSolInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    raffleState: TAccountMetas[0];
    solRaffle: TAccountMetas[1];
    round: TAccountMetas[2];
    roundTickets: TAccountMetas[3];
    rentVault: TAccountMetas[4];
    roundTicketsPurchase: TAccountMetas[5];
    player: TAccountMetas[6];
    vault: TAccountMetas[7];
    btcPriceFeed: TAccountMetas[8];
    solPriceFeed: TAccountMetas[9];
    systemProgram: TAccountMetas[10];
  };
  data: BuyTicketsSolInstructionData;
};

export function parseBuyTicketsSolInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedBuyTicketsSolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 11) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      raffleState: getNextAccount(),
      solRaffle: getNextAccount(),
      round: getNextAccount(),
      roundTickets: getNextAccount(),
      rentVault: getNextAccount(),
      roundTicketsPurchase: getNextAccount(),
      player: getNextAccount(),
      vault: getNextAccount(),
      btcPriceFeed: getNextAccount(),
      solPriceFeed: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getBuyTicketsSolInstructionDataDecoder().decode(instruction.data),
  };
}
