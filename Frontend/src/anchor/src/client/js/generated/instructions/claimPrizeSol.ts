/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  combineCodec,
  fixDecoderSize,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  transformEncoder,
  type WritableAccount,
  type WritableSignerAccount,
} from "gill";
import { u32ToLittleEndianBuffer } from "utils/buffer";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { expectAddress, getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CLAIM_PRIZE_SOL_DISCRIMINATOR = new Uint8Array([30, 87, 95, 109, 109, 76, 78, 2]);

export function getClaimPrizeSolDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLAIM_PRIZE_SOL_DISCRIMINATOR);
}

export type ClaimPrizeSolInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountRaffleState extends string | AccountMeta<string> = string,
  TAccountSolRaffle extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountRoundTickets extends string | AccountMeta<string> = string,
  TAccountWinner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountRentVault extends string | AccountMeta<string> = string,
  TAccountBeneficiary extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRaffleState extends string ? WritableAccount<TAccountRaffleState> : TAccountRaffleState,
      TAccountSolRaffle extends string ? WritableAccount<TAccountSolRaffle> : TAccountSolRaffle,
      TAccountRound extends string ? WritableAccount<TAccountRound> : TAccountRound,
      TAccountRoundTickets extends string ? WritableAccount<TAccountRoundTickets> : TAccountRoundTickets,
      TAccountWinner extends string
        ? WritableSignerAccount<TAccountWinner> & AccountSignerMeta<TAccountWinner>
        : TAccountWinner,
      TAccountVault extends string ? WritableAccount<TAccountVault> : TAccountVault,
      TAccountRentVault extends string ? WritableAccount<TAccountRentVault> : TAccountRentVault,
      TAccountBeneficiary extends string ? WritableAccount<TAccountBeneficiary> : TAccountBeneficiary,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimPrizeSolInstructionData = {
  discriminator: ReadonlyUint8Array;
  roundId: number;
};

export type ClaimPrizeSolInstructionDataArgs = { roundId: number };

export function getClaimPrizeSolInstructionDataEncoder(): FixedSizeEncoder<ClaimPrizeSolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["roundId", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CLAIM_PRIZE_SOL_DISCRIMINATOR }),
  );
}

export function getClaimPrizeSolInstructionDataDecoder(): FixedSizeDecoder<ClaimPrizeSolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["roundId", getU32Decoder()],
  ]);
}

export function getClaimPrizeSolInstructionDataCodec(): FixedSizeCodec<
  ClaimPrizeSolInstructionDataArgs,
  ClaimPrizeSolInstructionData
> {
  return combineCodec(getClaimPrizeSolInstructionDataEncoder(), getClaimPrizeSolInstructionDataDecoder());
}

export type ClaimPrizeSolAsyncInput<
  TAccountRaffleState extends string = string,
  TAccountSolRaffle extends string = string,
  TAccountRound extends string = string,
  TAccountRoundTickets extends string = string,
  TAccountWinner extends string = string,
  TAccountVault extends string = string,
  TAccountRentVault extends string = string,
  TAccountBeneficiary extends string = string,
> = {
  raffleState?: Address<TAccountRaffleState>;
  solRaffle?: Address<TAccountSolRaffle>;
  round?: Address<TAccountRound>;
  roundTickets?: Address<TAccountRoundTickets>;
  winner: TransactionSigner<TAccountWinner>;
  vault?: Address<TAccountVault>;
  rentVault?: Address<TAccountRentVault>;
  beneficiary: Address<TAccountBeneficiary>;
  roundId: ClaimPrizeSolInstructionDataArgs["roundId"];
};

export async function getClaimPrizeSolInstructionAsync<
  TAccountRaffleState extends string,
  TAccountSolRaffle extends string,
  TAccountRound extends string,
  TAccountRoundTickets extends string,
  TAccountWinner extends string,
  TAccountVault extends string,
  TAccountRentVault extends string,
  TAccountBeneficiary extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: ClaimPrizeSolAsyncInput<
    TAccountRaffleState,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTickets,
    TAccountWinner,
    TAccountVault,
    TAccountRentVault,
    TAccountBeneficiary
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClaimPrizeSolInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTickets,
    TAccountWinner,
    TAccountVault,
    TAccountRentVault,
    TAccountBeneficiary
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: true },
    solRaffle: { value: input.solRaffle ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    roundTickets: { value: input.roundTickets ?? null, isWritable: true },
    winner: { value: input.winner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    rentVault: { value: input.rentVault ?? null, isWritable: true },
    beneficiary: { value: input.beneficiary ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.raffleState.value) {
    accounts.raffleState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([114, 97, 102, 102, 108, 101, 95, 115, 116, 97, 116, 101]))],
    });
  }
  if (!accounts.solRaffle.value) {
    accounts.solRaffle.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([115, 111, 108, 95, 114, 97, 102, 102, 108, 101]))],
    });
  }
  if (!accounts.round.value) {
    accounts.round.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([114, 111, 117, 110, 100])),
        getAddressEncoder().encode(expectAddress(accounts.solRaffle.value)),
        u32ToLittleEndianBuffer(args.roundId),
      ],
    });
  }
  if (!accounts.roundTickets.value) {
    accounts.roundTickets.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([114, 111, 117, 110, 100, 95, 116, 105, 99, 107, 101, 116, 115])),
        getAddressEncoder().encode(expectAddress(accounts.round.value)),
      ],
    });
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([115, 111, 108, 95, 118, 97, 117, 108, 116]))],
    });
  }
  if (!accounts.rentVault.value) {
    accounts.rentVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([114, 101, 110, 116, 95, 118, 97, 117, 108, 116]))],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.raffleState),
      getAccountMeta(accounts.solRaffle),
      getAccountMeta(accounts.round),
      getAccountMeta(accounts.roundTickets),
      getAccountMeta(accounts.winner),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.rentVault),
      getAccountMeta(accounts.beneficiary),
    ],
    data: getClaimPrizeSolInstructionDataEncoder().encode(args as ClaimPrizeSolInstructionDataArgs),
    programAddress,
  } as ClaimPrizeSolInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTickets,
    TAccountWinner,
    TAccountVault,
    TAccountRentVault,
    TAccountBeneficiary
  >);
}

export type ClaimPrizeSolInput<
  TAccountRaffleState extends string = string,
  TAccountSolRaffle extends string = string,
  TAccountRound extends string = string,
  TAccountRoundTickets extends string = string,
  TAccountWinner extends string = string,
  TAccountVault extends string = string,
  TAccountRentVault extends string = string,
  TAccountBeneficiary extends string = string,
> = {
  raffleState: Address<TAccountRaffleState>;
  solRaffle: Address<TAccountSolRaffle>;
  round: Address<TAccountRound>;
  roundTickets: Address<TAccountRoundTickets>;
  winner: TransactionSigner<TAccountWinner>;
  vault: Address<TAccountVault>;
  rentVault: Address<TAccountRentVault>;
  beneficiary: Address<TAccountBeneficiary>;
  roundId: ClaimPrizeSolInstructionDataArgs["roundId"];
};

export function getClaimPrizeSolInstruction<
  TAccountRaffleState extends string,
  TAccountSolRaffle extends string,
  TAccountRound extends string,
  TAccountRoundTickets extends string,
  TAccountWinner extends string,
  TAccountVault extends string,
  TAccountRentVault extends string,
  TAccountBeneficiary extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: ClaimPrizeSolInput<
    TAccountRaffleState,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTickets,
    TAccountWinner,
    TAccountVault,
    TAccountRentVault,
    TAccountBeneficiary
  >,
  config?: { programAddress?: TProgramAddress },
): ClaimPrizeSolInstruction<
  TProgramAddress,
  TAccountRaffleState,
  TAccountSolRaffle,
  TAccountRound,
  TAccountRoundTickets,
  TAccountWinner,
  TAccountVault,
  TAccountRentVault,
  TAccountBeneficiary
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: true },
    solRaffle: { value: input.solRaffle ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    roundTickets: { value: input.roundTickets ?? null, isWritable: true },
    winner: { value: input.winner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    rentVault: { value: input.rentVault ?? null, isWritable: true },
    beneficiary: { value: input.beneficiary ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.raffleState),
      getAccountMeta(accounts.solRaffle),
      getAccountMeta(accounts.round),
      getAccountMeta(accounts.roundTickets),
      getAccountMeta(accounts.winner),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.rentVault),
      getAccountMeta(accounts.beneficiary),
    ],
    data: getClaimPrizeSolInstructionDataEncoder().encode(args as ClaimPrizeSolInstructionDataArgs),
    programAddress,
  } as ClaimPrizeSolInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTickets,
    TAccountWinner,
    TAccountVault,
    TAccountRentVault,
    TAccountBeneficiary
  >);
}

export type ParsedClaimPrizeSolInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    raffleState: TAccountMetas[0];
    solRaffle: TAccountMetas[1];
    round: TAccountMetas[2];
    roundTickets: TAccountMetas[3];
    winner: TAccountMetas[4];
    vault: TAccountMetas[5];
    rentVault: TAccountMetas[6];
    beneficiary: TAccountMetas[7];
  };
  data: ClaimPrizeSolInstructionData;
};

export function parseClaimPrizeSolInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedClaimPrizeSolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      raffleState: getNextAccount(),
      solRaffle: getNextAccount(),
      round: getNextAccount(),
      roundTickets: getNextAccount(),
      winner: getNextAccount(),
      vault: getNextAccount(),
      rentVault: getNextAccount(),
      beneficiary: getNextAccount(),
    },
    data: getClaimPrizeSolInstructionDataDecoder().decode(instruction.data),
  };
}
