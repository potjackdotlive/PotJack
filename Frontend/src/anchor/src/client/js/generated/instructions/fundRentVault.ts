/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "gill";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const FUND_RENT_VAULT_DISCRIMINATOR = new Uint8Array([
  244, 253, 189, 13, 179, 95, 31, 3,
]);

export function getFundRentVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FUND_RENT_VAULT_DISCRIMINATOR,
  );
}

export type FundRentVaultInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountRentVault extends string | AccountMeta<string> = string,
  TAccountFunder extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRentVault extends string
        ? WritableAccount<TAccountRentVault>
        : TAccountRentVault,
      TAccountFunder extends string
        ? WritableSignerAccount<TAccountFunder> &
            AccountSignerMeta<TAccountFunder>
        : TAccountFunder,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type FundRentVaultInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type FundRentVaultInstructionDataArgs = { amount: number | bigint };

export function getFundRentVaultInstructionDataEncoder(): FixedSizeEncoder<FundRentVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: FUND_RENT_VAULT_DISCRIMINATOR }),
  );
}

export function getFundRentVaultInstructionDataDecoder(): FixedSizeDecoder<FundRentVaultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getFundRentVaultInstructionDataCodec(): FixedSizeCodec<
  FundRentVaultInstructionDataArgs,
  FundRentVaultInstructionData
> {
  return combineCodec(
    getFundRentVaultInstructionDataEncoder(),
    getFundRentVaultInstructionDataDecoder(),
  );
}

export type FundRentVaultAsyncInput<
  TAccountRentVault extends string = string,
  TAccountFunder extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** PDA for storing funds fo rent */
  rentVault?: Address<TAccountRentVault>;
  funder: TransactionSigner<TAccountFunder>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: FundRentVaultInstructionDataArgs["amount"];
};

export async function getFundRentVaultInstructionAsync<
  TAccountRentVault extends string,
  TAccountFunder extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: FundRentVaultAsyncInput<
    TAccountRentVault,
    TAccountFunder,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  FundRentVaultInstruction<
    TProgramAddress,
    TAccountRentVault,
    TAccountFunder,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    rentVault: { value: input.rentVault ?? null, isWritable: true },
    funder: { value: input.funder ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.rentVault.value) {
    accounts.rentVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([114, 101, 110, 116, 95, 118, 97, 117, 108, 116]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.rentVault),
      getAccountMeta(accounts.funder),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getFundRentVaultInstructionDataEncoder().encode(
      args as FundRentVaultInstructionDataArgs,
    ),
    programAddress,
  } as FundRentVaultInstruction<
    TProgramAddress,
    TAccountRentVault,
    TAccountFunder,
    TAccountSystemProgram
  >);
}

export type FundRentVaultInput<
  TAccountRentVault extends string = string,
  TAccountFunder extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** PDA for storing funds fo rent */
  rentVault: Address<TAccountRentVault>;
  funder: TransactionSigner<TAccountFunder>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: FundRentVaultInstructionDataArgs["amount"];
};

export function getFundRentVaultInstruction<
  TAccountRentVault extends string,
  TAccountFunder extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: FundRentVaultInput<
    TAccountRentVault,
    TAccountFunder,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): FundRentVaultInstruction<
  TProgramAddress,
  TAccountRentVault,
  TAccountFunder,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    rentVault: { value: input.rentVault ?? null, isWritable: true },
    funder: { value: input.funder ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.rentVault),
      getAccountMeta(accounts.funder),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getFundRentVaultInstructionDataEncoder().encode(
      args as FundRentVaultInstructionDataArgs,
    ),
    programAddress,
  } as FundRentVaultInstruction<
    TProgramAddress,
    TAccountRentVault,
    TAccountFunder,
    TAccountSystemProgram
  >);
}

export type ParsedFundRentVaultInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** PDA for storing funds fo rent */
    rentVault: TAccountMetas[0];
    funder: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: FundRentVaultInstructionData;
};

export function parseFundRentVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFundRentVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      rentVault: getNextAccount(),
      funder: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getFundRentVaultInstructionDataDecoder().decode(instruction.data),
  };
}
