/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  type AccountMeta,
  type Address,
  combineCodec,
  fixDecoderSize,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  transformEncoder,
} from "gill";
import { u32ToLittleEndianBuffer } from "utils/buffer";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { expectAddress, getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const GET_CUMULATIVE_TICKETS_DISCRIMINATOR = new Uint8Array([64, 57, 10, 126, 172, 20, 56, 215]);

export function getGetCumulativeTicketsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GET_CUMULATIVE_TICKETS_DISCRIMINATOR);
}

export type GetCumulativeTicketsInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountSolRaffle extends string | AccountMeta<string> = string,
  TAccountRoundTickets extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRound extends string ? ReadonlyAccount<TAccountRound> : TAccountRound,
      TAccountSolRaffle extends string ? ReadonlyAccount<TAccountSolRaffle> : TAccountSolRaffle,
      TAccountRoundTickets extends string ? ReadonlyAccount<TAccountRoundTickets> : TAccountRoundTickets,
      ...TRemainingAccounts,
    ]
  >;

export type GetCumulativeTicketsInstructionData = {
  discriminator: ReadonlyUint8Array;
  roundId: number;
};

export type GetCumulativeTicketsInstructionDataArgs = { roundId: number };

export function getGetCumulativeTicketsInstructionDataEncoder(): FixedSizeEncoder<GetCumulativeTicketsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["roundId", getU32Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: GET_CUMULATIVE_TICKETS_DISCRIMINATOR,
    }),
  );
}

export function getGetCumulativeTicketsInstructionDataDecoder(): FixedSizeDecoder<GetCumulativeTicketsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["roundId", getU32Decoder()],
  ]);
}

export function getGetCumulativeTicketsInstructionDataCodec(): FixedSizeCodec<
  GetCumulativeTicketsInstructionDataArgs,
  GetCumulativeTicketsInstructionData
> {
  return combineCodec(getGetCumulativeTicketsInstructionDataEncoder(), getGetCumulativeTicketsInstructionDataDecoder());
}

export type GetCumulativeTicketsAsyncInput<
  TAccountRound extends string = string,
  TAccountSolRaffle extends string = string,
  TAccountRoundTickets extends string = string,
> = {
  round?: Address<TAccountRound>;
  solRaffle?: Address<TAccountSolRaffle>;
  roundTickets?: Address<TAccountRoundTickets>;
  roundId: GetCumulativeTicketsInstructionDataArgs["roundId"];
};

export async function getGetCumulativeTicketsInstructionAsync<
  TAccountRound extends string,
  TAccountSolRaffle extends string,
  TAccountRoundTickets extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: GetCumulativeTicketsAsyncInput<TAccountRound, TAccountSolRaffle, TAccountRoundTickets>,
  config?: { programAddress?: TProgramAddress },
): Promise<GetCumulativeTicketsInstruction<TProgramAddress, TAccountRound, TAccountSolRaffle, TAccountRoundTickets>> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    round: { value: input.round ?? null, isWritable: false },
    solRaffle: { value: input.solRaffle ?? null, isWritable: false },
    roundTickets: { value: input.roundTickets ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.solRaffle.value) {
    accounts.solRaffle.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([115, 111, 108, 95, 114, 97, 102, 102, 108, 101]))],
    });
  }
  if (!accounts.round.value) {
    accounts.round.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([114, 111, 117, 110, 100])),
        getAddressEncoder().encode(expectAddress(accounts.solRaffle.value)),
        u32ToLittleEndianBuffer(args.roundId),
      ],
    });
  }
  if (!accounts.roundTickets.value) {
    accounts.roundTickets.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([114, 111, 117, 110, 100, 95, 116, 105, 99, 107, 101, 116, 115])),
        getAddressEncoder().encode(expectAddress(accounts.round.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.round),
      getAccountMeta(accounts.solRaffle),
      getAccountMeta(accounts.roundTickets),
    ],
    data: getGetCumulativeTicketsInstructionDataEncoder().encode(args as GetCumulativeTicketsInstructionDataArgs),
    programAddress,
  } as GetCumulativeTicketsInstruction<TProgramAddress, TAccountRound, TAccountSolRaffle, TAccountRoundTickets>);
}

export type GetCumulativeTicketsInput<
  TAccountRound extends string = string,
  TAccountSolRaffle extends string = string,
  TAccountRoundTickets extends string = string,
> = {
  round: Address<TAccountRound>;
  solRaffle: Address<TAccountSolRaffle>;
  roundTickets: Address<TAccountRoundTickets>;
  roundId: GetCumulativeTicketsInstructionDataArgs["roundId"];
};

export function getGetCumulativeTicketsInstruction<
  TAccountRound extends string,
  TAccountSolRaffle extends string,
  TAccountRoundTickets extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: GetCumulativeTicketsInput<TAccountRound, TAccountSolRaffle, TAccountRoundTickets>,
  config?: { programAddress?: TProgramAddress },
): GetCumulativeTicketsInstruction<TProgramAddress, TAccountRound, TAccountSolRaffle, TAccountRoundTickets> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    round: { value: input.round ?? null, isWritable: false },
    solRaffle: { value: input.solRaffle ?? null, isWritable: false },
    roundTickets: { value: input.roundTickets ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.round),
      getAccountMeta(accounts.solRaffle),
      getAccountMeta(accounts.roundTickets),
    ],
    data: getGetCumulativeTicketsInstructionDataEncoder().encode(args as GetCumulativeTicketsInstructionDataArgs),
    programAddress,
  } as GetCumulativeTicketsInstruction<TProgramAddress, TAccountRound, TAccountSolRaffle, TAccountRoundTickets>);
}

export type ParsedGetCumulativeTicketsInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    round: TAccountMetas[0];
    solRaffle: TAccountMetas[1];
    roundTickets: TAccountMetas[2];
  };
  data: GetCumulativeTicketsInstructionData;
};

export function parseGetCumulativeTicketsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedGetCumulativeTicketsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      round: getNextAccount(),
      solRaffle: getNextAccount(),
      roundTickets: getNextAccount(),
    },
    data: getGetCumulativeTicketsInstructionDataDecoder().decode(instruction.data),
  };
}
