/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "gill";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const GET_CURRENT_RAFFLE_ROUND_ID_DISCRIMINATOR = new Uint8Array([
  141, 75, 189, 206, 206, 91, 23, 21,
]);

export function getGetCurrentRaffleRoundIdDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GET_CURRENT_RAFFLE_ROUND_ID_DISCRIMINATOR,
  );
}

export type GetCurrentRaffleRoundIdInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountSolRaffle extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSolRaffle extends string
        ? ReadonlyAccount<TAccountSolRaffle>
        : TAccountSolRaffle,
      ...TRemainingAccounts,
    ]
  >;

export type GetCurrentRaffleRoundIdInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type GetCurrentRaffleRoundIdInstructionDataArgs = {};

export function getGetCurrentRaffleRoundIdInstructionDataEncoder(): FixedSizeEncoder<GetCurrentRaffleRoundIdInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: GET_CURRENT_RAFFLE_ROUND_ID_DISCRIMINATOR,
    }),
  );
}

export function getGetCurrentRaffleRoundIdInstructionDataDecoder(): FixedSizeDecoder<GetCurrentRaffleRoundIdInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getGetCurrentRaffleRoundIdInstructionDataCodec(): FixedSizeCodec<
  GetCurrentRaffleRoundIdInstructionDataArgs,
  GetCurrentRaffleRoundIdInstructionData
> {
  return combineCodec(
    getGetCurrentRaffleRoundIdInstructionDataEncoder(),
    getGetCurrentRaffleRoundIdInstructionDataDecoder(),
  );
}

export type GetCurrentRaffleRoundIdAsyncInput<
  TAccountSolRaffle extends string = string,
> = {
  solRaffle?: Address<TAccountSolRaffle>;
};

export async function getGetCurrentRaffleRoundIdInstructionAsync<
  TAccountSolRaffle extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: GetCurrentRaffleRoundIdAsyncInput<TAccountSolRaffle>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  GetCurrentRaffleRoundIdInstruction<TProgramAddress, TAccountSolRaffle>
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    solRaffle: { value: input.solRaffle ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.solRaffle.value) {
    accounts.solRaffle.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([115, 111, 108, 95, 114, 97, 102, 102, 108, 101]),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.solRaffle)],
    data: getGetCurrentRaffleRoundIdInstructionDataEncoder().encode({}),
    programAddress,
  } as GetCurrentRaffleRoundIdInstruction<TProgramAddress, TAccountSolRaffle>);
}

export type GetCurrentRaffleRoundIdInput<
  TAccountSolRaffle extends string = string,
> = {
  solRaffle: Address<TAccountSolRaffle>;
};

export function getGetCurrentRaffleRoundIdInstruction<
  TAccountSolRaffle extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: GetCurrentRaffleRoundIdInput<TAccountSolRaffle>,
  config?: { programAddress?: TProgramAddress },
): GetCurrentRaffleRoundIdInstruction<TProgramAddress, TAccountSolRaffle> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    solRaffle: { value: input.solRaffle ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.solRaffle)],
    data: getGetCurrentRaffleRoundIdInstructionDataEncoder().encode({}),
    programAddress,
  } as GetCurrentRaffleRoundIdInstruction<TProgramAddress, TAccountSolRaffle>);
}

export type ParsedGetCurrentRaffleRoundIdInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    solRaffle: TAccountMetas[0];
  };
  data: GetCurrentRaffleRoundIdInstructionData;
};

export function parseGetCurrentRaffleRoundIdInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGetCurrentRaffleRoundIdInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { solRaffle: getNextAccount() },
    data: getGetCurrentRaffleRoundIdInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
