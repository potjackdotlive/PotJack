/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "gill";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const GET_RAFFLE_ROUND_COUNT_DISCRIMINATOR = new Uint8Array([
  176, 188, 197, 73, 66, 119, 61, 5,
]);

export function getGetRaffleRoundCountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GET_RAFFLE_ROUND_COUNT_DISCRIMINATOR,
  );
}

export type GetRaffleRoundCountInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountSolRaffle extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSolRaffle extends string
        ? ReadonlyAccount<TAccountSolRaffle>
        : TAccountSolRaffle,
      ...TRemainingAccounts,
    ]
  >;

export type GetRaffleRoundCountInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type GetRaffleRoundCountInstructionDataArgs = {};

export function getGetRaffleRoundCountInstructionDataEncoder(): FixedSizeEncoder<GetRaffleRoundCountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: GET_RAFFLE_ROUND_COUNT_DISCRIMINATOR,
    }),
  );
}

export function getGetRaffleRoundCountInstructionDataDecoder(): FixedSizeDecoder<GetRaffleRoundCountInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getGetRaffleRoundCountInstructionDataCodec(): FixedSizeCodec<
  GetRaffleRoundCountInstructionDataArgs,
  GetRaffleRoundCountInstructionData
> {
  return combineCodec(
    getGetRaffleRoundCountInstructionDataEncoder(),
    getGetRaffleRoundCountInstructionDataDecoder(),
  );
}

export type GetRaffleRoundCountAsyncInput<
  TAccountSolRaffle extends string = string,
> = {
  solRaffle?: Address<TAccountSolRaffle>;
};

export async function getGetRaffleRoundCountInstructionAsync<
  TAccountSolRaffle extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: GetRaffleRoundCountAsyncInput<TAccountSolRaffle>,
  config?: { programAddress?: TProgramAddress },
): Promise<GetRaffleRoundCountInstruction<TProgramAddress, TAccountSolRaffle>> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    solRaffle: { value: input.solRaffle ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.solRaffle.value) {
    accounts.solRaffle.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([115, 111, 108, 95, 114, 97, 102, 102, 108, 101]),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.solRaffle)],
    data: getGetRaffleRoundCountInstructionDataEncoder().encode({}),
    programAddress,
  } as GetRaffleRoundCountInstruction<TProgramAddress, TAccountSolRaffle>);
}

export type GetRaffleRoundCountInput<
  TAccountSolRaffle extends string = string,
> = {
  solRaffle: Address<TAccountSolRaffle>;
};

export function getGetRaffleRoundCountInstruction<
  TAccountSolRaffle extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: GetRaffleRoundCountInput<TAccountSolRaffle>,
  config?: { programAddress?: TProgramAddress },
): GetRaffleRoundCountInstruction<TProgramAddress, TAccountSolRaffle> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    solRaffle: { value: input.solRaffle ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.solRaffle)],
    data: getGetRaffleRoundCountInstructionDataEncoder().encode({}),
    programAddress,
  } as GetRaffleRoundCountInstruction<TProgramAddress, TAccountSolRaffle>);
}

export type ParsedGetRaffleRoundCountInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    solRaffle: TAccountMetas[0];
  };
  data: GetRaffleRoundCountInstructionData;
};

export function parseGetRaffleRoundCountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGetRaffleRoundCountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { solRaffle: getNextAccount() },
    data: getGetRaffleRoundCountInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
