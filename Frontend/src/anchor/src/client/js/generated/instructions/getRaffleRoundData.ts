/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  type AccountMeta,
  type Address,
  combineCodec,
  fixDecoderSize,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  transformEncoder,
} from "gill";
import { u32ToLittleEndianBuffer } from "utils/buffer";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { expectAddress, getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const GET_RAFFLE_ROUND_DATA_DISCRIMINATOR = new Uint8Array([47, 97, 138, 249, 89, 171, 155, 159]);

export function getGetRaffleRoundDataDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GET_RAFFLE_ROUND_DATA_DISCRIMINATOR);
}

export type GetRaffleRoundDataInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountSolRaffle extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSolRaffle extends string ? ReadonlyAccount<TAccountSolRaffle> : TAccountSolRaffle,
      TAccountRound extends string ? ReadonlyAccount<TAccountRound> : TAccountRound,
      ...TRemainingAccounts,
    ]
  >;

export type GetRaffleRoundDataInstructionData = {
  discriminator: ReadonlyUint8Array;
  roundId: number;
};

export type GetRaffleRoundDataInstructionDataArgs = { roundId: number };

export function getGetRaffleRoundDataInstructionDataEncoder(): FixedSizeEncoder<GetRaffleRoundDataInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["roundId", getU32Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: GET_RAFFLE_ROUND_DATA_DISCRIMINATOR,
    }),
  );
}

export function getGetRaffleRoundDataInstructionDataDecoder(): FixedSizeDecoder<GetRaffleRoundDataInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["roundId", getU32Decoder()],
  ]);
}

export function getGetRaffleRoundDataInstructionDataCodec(): FixedSizeCodec<
  GetRaffleRoundDataInstructionDataArgs,
  GetRaffleRoundDataInstructionData
> {
  return combineCodec(getGetRaffleRoundDataInstructionDataEncoder(), getGetRaffleRoundDataInstructionDataDecoder());
}

export type GetRaffleRoundDataAsyncInput<
  TAccountSolRaffle extends string = string,
  TAccountRound extends string = string,
> = {
  solRaffle?: Address<TAccountSolRaffle>;
  round?: Address<TAccountRound>;
  roundId: GetRaffleRoundDataInstructionDataArgs["roundId"];
};

export async function getGetRaffleRoundDataInstructionAsync<
  TAccountSolRaffle extends string,
  TAccountRound extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: GetRaffleRoundDataAsyncInput<TAccountSolRaffle, TAccountRound>,
  config?: { programAddress?: TProgramAddress },
): Promise<GetRaffleRoundDataInstruction<TProgramAddress, TAccountSolRaffle, TAccountRound>> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    solRaffle: { value: input.solRaffle ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.solRaffle.value) {
    accounts.solRaffle.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([115, 111, 108, 95, 114, 97, 102, 102, 108, 101]))],
    });
  }
  if (!accounts.round.value) {
    accounts.round.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([114, 111, 117, 110, 100])),
        getAddressEncoder().encode(expectAddress(accounts.solRaffle.value)),
        u32ToLittleEndianBuffer(args.roundId),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.solRaffle), getAccountMeta(accounts.round)],
    data: getGetRaffleRoundDataInstructionDataEncoder().encode(args as GetRaffleRoundDataInstructionDataArgs),
    programAddress,
  } as GetRaffleRoundDataInstruction<TProgramAddress, TAccountSolRaffle, TAccountRound>);
}

export type GetRaffleRoundDataInput<
  TAccountSolRaffle extends string = string,
  TAccountRound extends string = string,
> = {
  solRaffle: Address<TAccountSolRaffle>;
  round: Address<TAccountRound>;
  roundId: GetRaffleRoundDataInstructionDataArgs["roundId"];
};

export function getGetRaffleRoundDataInstruction<
  TAccountSolRaffle extends string,
  TAccountRound extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: GetRaffleRoundDataInput<TAccountSolRaffle, TAccountRound>,
  config?: { programAddress?: TProgramAddress },
): GetRaffleRoundDataInstruction<TProgramAddress, TAccountSolRaffle, TAccountRound> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    solRaffle: { value: input.solRaffle ?? null, isWritable: false },
    round: { value: input.round ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.solRaffle), getAccountMeta(accounts.round)],
    data: getGetRaffleRoundDataInstructionDataEncoder().encode(args as GetRaffleRoundDataInstructionDataArgs),
    programAddress,
  } as GetRaffleRoundDataInstruction<TProgramAddress, TAccountSolRaffle, TAccountRound>);
}

export type ParsedGetRaffleRoundDataInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    solRaffle: TAccountMetas[0];
    round: TAccountMetas[1];
  };
  data: GetRaffleRoundDataInstructionData;
};

export function parseGetRaffleRoundDataInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedGetRaffleRoundDataInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { solRaffle: getNextAccount(), round: getNextAccount() },
    data: getGetRaffleRoundDataInstructionDataDecoder().decode(instruction.data),
  };
}
