/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "gill";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const GET_RAFFLE_STATE_DISCRIMINATOR = new Uint8Array([
  239, 174, 166, 93, 167, 51, 94, 224,
]);

export function getGetRaffleStateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GET_RAFFLE_STATE_DISCRIMINATOR,
  );
}

export type GetRaffleStateInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountRaffleState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRaffleState extends string
        ? ReadonlyAccount<TAccountRaffleState>
        : TAccountRaffleState,
      ...TRemainingAccounts,
    ]
  >;

export type GetRaffleStateInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type GetRaffleStateInstructionDataArgs = {};

export function getGetRaffleStateInstructionDataEncoder(): FixedSizeEncoder<GetRaffleStateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: GET_RAFFLE_STATE_DISCRIMINATOR }),
  );
}

export function getGetRaffleStateInstructionDataDecoder(): FixedSizeDecoder<GetRaffleStateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getGetRaffleStateInstructionDataCodec(): FixedSizeCodec<
  GetRaffleStateInstructionDataArgs,
  GetRaffleStateInstructionData
> {
  return combineCodec(
    getGetRaffleStateInstructionDataEncoder(),
    getGetRaffleStateInstructionDataDecoder(),
  );
}

export type GetRaffleStateAsyncInput<
  TAccountRaffleState extends string = string,
> = {
  raffleState?: Address<TAccountRaffleState>;
};

export async function getGetRaffleStateInstructionAsync<
  TAccountRaffleState extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: GetRaffleStateAsyncInput<TAccountRaffleState>,
  config?: { programAddress?: TProgramAddress },
): Promise<GetRaffleStateInstruction<TProgramAddress, TAccountRaffleState>> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.raffleState.value) {
    accounts.raffleState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 97, 102, 102, 108, 101, 95, 115, 116, 97, 116, 101,
          ]),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.raffleState)],
    data: getGetRaffleStateInstructionDataEncoder().encode({}),
    programAddress,
  } as GetRaffleStateInstruction<TProgramAddress, TAccountRaffleState>);
}

export type GetRaffleStateInput<TAccountRaffleState extends string = string> = {
  raffleState: Address<TAccountRaffleState>;
};

export function getGetRaffleStateInstruction<
  TAccountRaffleState extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: GetRaffleStateInput<TAccountRaffleState>,
  config?: { programAddress?: TProgramAddress },
): GetRaffleStateInstruction<TProgramAddress, TAccountRaffleState> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.raffleState)],
    data: getGetRaffleStateInstructionDataEncoder().encode({}),
    programAddress,
  } as GetRaffleStateInstruction<TProgramAddress, TAccountRaffleState>);
}

export type ParsedGetRaffleStateInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    raffleState: TAccountMetas[0];
  };
  data: GetRaffleStateInstructionData;
};

export function parseGetRaffleStateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGetRaffleStateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { raffleState: getNextAccount() },
    data: getGetRaffleStateInstructionDataDecoder().decode(instruction.data),
  };
}
