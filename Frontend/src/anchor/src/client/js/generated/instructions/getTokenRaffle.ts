/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "gill";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const GET_TOKEN_RAFFLE_DISCRIMINATOR = new Uint8Array([
  182, 225, 154, 248, 119, 241, 162, 65,
]);

export function getGetTokenRaffleDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GET_TOKEN_RAFFLE_DISCRIMINATOR,
  );
}

export type GetTokenRaffleInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountSolRaffle extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSolRaffle extends string
        ? ReadonlyAccount<TAccountSolRaffle>
        : TAccountSolRaffle,
      ...TRemainingAccounts,
    ]
  >;

export type GetTokenRaffleInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type GetTokenRaffleInstructionDataArgs = {};

export function getGetTokenRaffleInstructionDataEncoder(): FixedSizeEncoder<GetTokenRaffleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: GET_TOKEN_RAFFLE_DISCRIMINATOR }),
  );
}

export function getGetTokenRaffleInstructionDataDecoder(): FixedSizeDecoder<GetTokenRaffleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getGetTokenRaffleInstructionDataCodec(): FixedSizeCodec<
  GetTokenRaffleInstructionDataArgs,
  GetTokenRaffleInstructionData
> {
  return combineCodec(
    getGetTokenRaffleInstructionDataEncoder(),
    getGetTokenRaffleInstructionDataDecoder(),
  );
}

export type GetTokenRaffleAsyncInput<
  TAccountSolRaffle extends string = string,
> = {
  solRaffle?: Address<TAccountSolRaffle>;
};

export async function getGetTokenRaffleInstructionAsync<
  TAccountSolRaffle extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: GetTokenRaffleAsyncInput<TAccountSolRaffle>,
  config?: { programAddress?: TProgramAddress },
): Promise<GetTokenRaffleInstruction<TProgramAddress, TAccountSolRaffle>> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    solRaffle: { value: input.solRaffle ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.solRaffle.value) {
    accounts.solRaffle.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([115, 111, 108, 95, 114, 97, 102, 102, 108, 101]),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.solRaffle)],
    data: getGetTokenRaffleInstructionDataEncoder().encode({}),
    programAddress,
  } as GetTokenRaffleInstruction<TProgramAddress, TAccountSolRaffle>);
}

export type GetTokenRaffleInput<TAccountSolRaffle extends string = string> = {
  solRaffle: Address<TAccountSolRaffle>;
};

export function getGetTokenRaffleInstruction<
  TAccountSolRaffle extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: GetTokenRaffleInput<TAccountSolRaffle>,
  config?: { programAddress?: TProgramAddress },
): GetTokenRaffleInstruction<TProgramAddress, TAccountSolRaffle> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    solRaffle: { value: input.solRaffle ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.solRaffle)],
    data: getGetTokenRaffleInstructionDataEncoder().encode({}),
    programAddress,
  } as GetTokenRaffleInstruction<TProgramAddress, TAccountSolRaffle>);
}

export type ParsedGetTokenRaffleInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    solRaffle: TAccountMetas[0];
  };
  data: GetTokenRaffleInstructionData;
};

export function parseGetTokenRaffleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGetTokenRaffleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { solRaffle: getNextAccount() },
    data: getGetTokenRaffleInstructionDataDecoder().decode(instruction.data),
  };
}
