/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "gill";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const INITIALIZE_CLIENT_STATE_DISCRIMINATOR = new Uint8Array([
  135, 57, 192, 113, 176, 11, 209, 132,
]);

export function getInitializeClientStateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_CLIENT_STATE_DISCRIMINATOR,
  );
}

export type InitializeClientStateInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountClientState extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountClientState extends string
        ? WritableAccount<TAccountClientState>
        : TAccountClientState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeClientStateInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeClientStateInstructionDataArgs = {};

export function getInitializeClientStateInstructionDataEncoder(): FixedSizeEncoder<InitializeClientStateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_CLIENT_STATE_DISCRIMINATOR,
    }),
  );
}

export function getInitializeClientStateInstructionDataDecoder(): FixedSizeDecoder<InitializeClientStateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeClientStateInstructionDataCodec(): FixedSizeCodec<
  InitializeClientStateInstructionDataArgs,
  InitializeClientStateInstructionData
> {
  return combineCodec(
    getInitializeClientStateInstructionDataEncoder(),
    getInitializeClientStateInstructionDataDecoder(),
  );
}

export type InitializeClientStateAsyncInput<
  TAccountAuthority extends string = string,
  TAccountClientState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  clientState?: Address<TAccountClientState>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializeClientStateInstructionAsync<
  TAccountAuthority extends string,
  TAccountClientState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: InitializeClientStateAsyncInput<
    TAccountAuthority,
    TAccountClientState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeClientStateInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountClientState,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    clientState: { value: input.clientState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.clientState.value) {
    accounts.clientState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([67, 76, 73, 69, 78, 84, 95, 83, 84, 65, 84, 69]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.clientState),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeClientStateInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeClientStateInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountClientState,
    TAccountSystemProgram
  >);
}

export type InitializeClientStateInput<
  TAccountAuthority extends string = string,
  TAccountClientState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  clientState: Address<TAccountClientState>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeClientStateInstruction<
  TAccountAuthority extends string,
  TAccountClientState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: InitializeClientStateInput<
    TAccountAuthority,
    TAccountClientState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeClientStateInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountClientState,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    clientState: { value: input.clientState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.clientState),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeClientStateInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeClientStateInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountClientState,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeClientStateInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    clientState: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeClientStateInstructionData;
};

export function parseInitializeClientStateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeClientStateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      clientState: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeClientStateInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
