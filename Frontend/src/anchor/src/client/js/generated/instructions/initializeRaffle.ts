/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "gill";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const INITIALIZE_RAFFLE_DISCRIMINATOR = new Uint8Array([
  110, 142, 92, 16, 15, 58, 89, 229,
]);

export function getInitializeRaffleDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_RAFFLE_DISCRIMINATOR,
  );
}

export type InitializeRaffleInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountRaffleState extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRaffleState extends string
        ? WritableAccount<TAccountRaffleState>
        : TAccountRaffleState,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeRaffleInstructionData = {
  discriminator: ReadonlyUint8Array;
  entranceFeePercentage: number;
  beneficiary: Address;
};

export type InitializeRaffleInstructionDataArgs = {
  entranceFeePercentage: number;
  beneficiary: Address;
};

export function getInitializeRaffleInstructionDataEncoder(): FixedSizeEncoder<InitializeRaffleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["entranceFeePercentage", getU8Encoder()],
      ["beneficiary", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_RAFFLE_DISCRIMINATOR }),
  );
}

export function getInitializeRaffleInstructionDataDecoder(): FixedSizeDecoder<InitializeRaffleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["entranceFeePercentage", getU8Decoder()],
    ["beneficiary", getAddressDecoder()],
  ]);
}

export function getInitializeRaffleInstructionDataCodec(): FixedSizeCodec<
  InitializeRaffleInstructionDataArgs,
  InitializeRaffleInstructionData
> {
  return combineCodec(
    getInitializeRaffleInstructionDataEncoder(),
    getInitializeRaffleInstructionDataDecoder(),
  );
}

export type InitializeRaffleAsyncInput<
  TAccountRaffleState extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountVault extends string = string,
> = {
  raffleState?: Address<TAccountRaffleState>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  vault?: Address<TAccountVault>;
  entranceFeePercentage: InitializeRaffleInstructionDataArgs["entranceFeePercentage"];
  beneficiary: InitializeRaffleInstructionDataArgs["beneficiary"];
};

export async function getInitializeRaffleInstructionAsync<
  TAccountRaffleState extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: InitializeRaffleAsyncInput<
    TAccountRaffleState,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountVault
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeRaffleInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountVault
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.raffleState.value) {
    accounts.raffleState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 97, 102, 102, 108, 101, 95, 115, 116, 97, 116, 101,
          ]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([115, 111, 108, 95, 118, 97, 117, 108, 116]),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.raffleState),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.vault),
    ],
    data: getInitializeRaffleInstructionDataEncoder().encode(
      args as InitializeRaffleInstructionDataArgs,
    ),
    programAddress,
  } as InitializeRaffleInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountVault
  >);
}

export type InitializeRaffleInput<
  TAccountRaffleState extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountVault extends string = string,
> = {
  raffleState: Address<TAccountRaffleState>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  vault: Address<TAccountVault>;
  entranceFeePercentage: InitializeRaffleInstructionDataArgs["entranceFeePercentage"];
  beneficiary: InitializeRaffleInstructionDataArgs["beneficiary"];
};

export function getInitializeRaffleInstruction<
  TAccountRaffleState extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: InitializeRaffleInput<
    TAccountRaffleState,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountVault
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeRaffleInstruction<
  TProgramAddress,
  TAccountRaffleState,
  TAccountAuthority,
  TAccountSystemProgram,
  TAccountVault
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.raffleState),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.vault),
    ],
    data: getInitializeRaffleInstructionDataEncoder().encode(
      args as InitializeRaffleInstructionDataArgs,
    ),
    programAddress,
  } as InitializeRaffleInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountVault
  >);
}

export type ParsedInitializeRaffleInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    raffleState: TAccountMetas[0];
    authority: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
    vault: TAccountMetas[3];
  };
  data: InitializeRaffleInstructionData;
};

export function parseInitializeRaffleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeRaffleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      raffleState: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
      vault: getNextAccount(),
    },
    data: getInitializeRaffleInstructionDataDecoder().decode(instruction.data),
  };
}
