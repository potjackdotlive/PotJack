/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  combineCodec,
  fixDecoderSize,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  transformEncoder,
  type WritableAccount,
  type WritableSignerAccount,
} from "gill";
import { u32ToLittleEndianBuffer } from "utils/buffer";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { expectAddress, getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const INITIALIZE_ROUND_DISCRIMINATOR = new Uint8Array([43, 135, 19, 93, 14, 225, 131, 188]);

export function getInitializeRoundDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INITIALIZE_ROUND_DISCRIMINATOR);
}

export type InitializeRoundInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountSolRaffle extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountRoundTickets extends string | AccountMeta<string> = string,
  TAccountRentVault extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSolRaffle extends string ? WritableAccount<TAccountSolRaffle> : TAccountSolRaffle,
      TAccountRound extends string ? WritableAccount<TAccountRound> : TAccountRound,
      TAccountRoundTickets extends string ? WritableAccount<TAccountRoundTickets> : TAccountRoundTickets,
      TAccountRentVault extends string ? WritableAccount<TAccountRentVault> : TAccountRentVault,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeRoundInstructionData = {
  discriminator: ReadonlyUint8Array;
  roundId: number;
};

export type InitializeRoundInstructionDataArgs = { roundId: number };

export function getInitializeRoundInstructionDataEncoder(): FixedSizeEncoder<InitializeRoundInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["roundId", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_ROUND_DISCRIMINATOR }),
  );
}

export function getInitializeRoundInstructionDataDecoder(): FixedSizeDecoder<InitializeRoundInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["roundId", getU32Decoder()],
  ]);
}

export function getInitializeRoundInstructionDataCodec(): FixedSizeCodec<
  InitializeRoundInstructionDataArgs,
  InitializeRoundInstructionData
> {
  return combineCodec(getInitializeRoundInstructionDataEncoder(), getInitializeRoundInstructionDataDecoder());
}

export type InitializeRoundAsyncInput<
  TAccountSolRaffle extends string = string,
  TAccountRound extends string = string,
  TAccountRoundTickets extends string = string,
  TAccountRentVault extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  solRaffle?: Address<TAccountSolRaffle>;
  round?: Address<TAccountRound>;
  roundTickets?: Address<TAccountRoundTickets>;
  rentVault?: Address<TAccountRentVault>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  roundId: InitializeRoundInstructionDataArgs["roundId"];
};

export async function getInitializeRoundInstructionAsync<
  TAccountSolRaffle extends string,
  TAccountRound extends string,
  TAccountRoundTickets extends string,
  TAccountRentVault extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: InitializeRoundAsyncInput<
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTickets,
    TAccountRentVault,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeRoundInstruction<
    TProgramAddress,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTickets,
    TAccountRentVault,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    solRaffle: { value: input.solRaffle ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    roundTickets: { value: input.roundTickets ?? null, isWritable: true },
    rentVault: { value: input.rentVault ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.solRaffle.value) {
    accounts.solRaffle.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([115, 111, 108, 95, 114, 97, 102, 102, 108, 101]))],
    });
  }
  if (!accounts.round.value) {
    accounts.round.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([114, 111, 117, 110, 100])),
        getAddressEncoder().encode(expectAddress(accounts.solRaffle.value)),
        u32ToLittleEndianBuffer(args.roundId),
      ],
    });
  }
  if (!accounts.roundTickets.value) {
    accounts.roundTickets.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([114, 111, 117, 110, 100, 95, 116, 105, 99, 107, 101, 116, 115])),
        getAddressEncoder().encode(expectAddress(accounts.round.value)),
      ],
    });
  }
  if (!accounts.rentVault.value) {
    accounts.rentVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([114, 101, 110, 116, 95, 118, 97, 117, 108, 116]))],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.solRaffle),
      getAccountMeta(accounts.round),
      getAccountMeta(accounts.roundTickets),
      getAccountMeta(accounts.rentVault),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeRoundInstructionDataEncoder().encode(args as InitializeRoundInstructionDataArgs),
    programAddress,
  } as InitializeRoundInstruction<
    TProgramAddress,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTickets,
    TAccountRentVault,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type InitializeRoundInput<
  TAccountSolRaffle extends string = string,
  TAccountRound extends string = string,
  TAccountRoundTickets extends string = string,
  TAccountRentVault extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  solRaffle: Address<TAccountSolRaffle>;
  round: Address<TAccountRound>;
  roundTickets: Address<TAccountRoundTickets>;
  rentVault: Address<TAccountRentVault>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  roundId: InitializeRoundInstructionDataArgs["roundId"];
};

export function getInitializeRoundInstruction<
  TAccountSolRaffle extends string,
  TAccountRound extends string,
  TAccountRoundTickets extends string,
  TAccountRentVault extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: InitializeRoundInput<
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTickets,
    TAccountRentVault,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeRoundInstruction<
  TProgramAddress,
  TAccountSolRaffle,
  TAccountRound,
  TAccountRoundTickets,
  TAccountRentVault,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    solRaffle: { value: input.solRaffle ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    roundTickets: { value: input.roundTickets ?? null, isWritable: true },
    rentVault: { value: input.rentVault ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.solRaffle),
      getAccountMeta(accounts.round),
      getAccountMeta(accounts.roundTickets),
      getAccountMeta(accounts.rentVault),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeRoundInstructionDataEncoder().encode(args as InitializeRoundInstructionDataArgs),
    programAddress,
  } as InitializeRoundInstruction<
    TProgramAddress,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTickets,
    TAccountRentVault,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeRoundInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    solRaffle: TAccountMetas[0];
    round: TAccountMetas[1];
    roundTickets: TAccountMetas[2];
    rentVault: TAccountMetas[3];
    authority: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: InitializeRoundInstructionData;
};

export function parseInitializeRoundInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeRoundInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      solRaffle: getNextAccount(),
      round: getNextAccount(),
      roundTickets: getNextAccount(),
      rentVault: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeRoundInstructionDataDecoder().decode(instruction.data),
  };
}
