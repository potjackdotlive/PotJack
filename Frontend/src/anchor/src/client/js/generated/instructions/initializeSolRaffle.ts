/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "gill";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const INITIALIZE_SOL_RAFFLE_DISCRIMINATOR = new Uint8Array([
  73, 0, 235, 207, 121, 115, 50, 23,
]);

export function getInitializeSolRaffleDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_SOL_RAFFLE_DISCRIMINATOR,
  );
}

export type InitializeSolRaffleInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSolRaffle extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSolRaffle extends string
        ? WritableAccount<TAccountSolRaffle>
        : TAccountSolRaffle,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeSolRaffleInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeSolRaffleInstructionDataArgs = {};

export function getInitializeSolRaffleInstructionDataEncoder(): FixedSizeEncoder<InitializeSolRaffleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_SOL_RAFFLE_DISCRIMINATOR,
    }),
  );
}

export function getInitializeSolRaffleInstructionDataDecoder(): FixedSizeDecoder<InitializeSolRaffleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeSolRaffleInstructionDataCodec(): FixedSizeCodec<
  InitializeSolRaffleInstructionDataArgs,
  InitializeSolRaffleInstructionData
> {
  return combineCodec(
    getInitializeSolRaffleInstructionDataEncoder(),
    getInitializeSolRaffleInstructionDataDecoder(),
  );
}

export type InitializeSolRaffleAsyncInput<
  TAccountAuthority extends string = string,
  TAccountSolRaffle extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  solRaffle?: Address<TAccountSolRaffle>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializeSolRaffleInstructionAsync<
  TAccountAuthority extends string,
  TAccountSolRaffle extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: InitializeSolRaffleAsyncInput<
    TAccountAuthority,
    TAccountSolRaffle,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeSolRaffleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSolRaffle,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    solRaffle: { value: input.solRaffle ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.solRaffle.value) {
    accounts.solRaffle.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([115, 111, 108, 95, 114, 97, 102, 102, 108, 101]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.solRaffle),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeSolRaffleInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeSolRaffleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSolRaffle,
    TAccountSystemProgram
  >);
}

export type InitializeSolRaffleInput<
  TAccountAuthority extends string = string,
  TAccountSolRaffle extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  solRaffle: Address<TAccountSolRaffle>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeSolRaffleInstruction<
  TAccountAuthority extends string,
  TAccountSolRaffle extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: InitializeSolRaffleInput<
    TAccountAuthority,
    TAccountSolRaffle,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeSolRaffleInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountSolRaffle,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    solRaffle: { value: input.solRaffle ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.solRaffle),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeSolRaffleInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeSolRaffleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSolRaffle,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeSolRaffleInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    solRaffle: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeSolRaffleInstructionData;
};

export function parseInitializeSolRaffleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeSolRaffleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      solRaffle: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeSolRaffleInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
