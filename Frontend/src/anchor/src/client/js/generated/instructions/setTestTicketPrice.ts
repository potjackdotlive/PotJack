/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "gill";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_TEST_TICKET_PRICE_DISCRIMINATOR = new Uint8Array([
  143, 243, 124, 103, 50, 219, 62, 40,
]);

export function getSetTestTicketPriceDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_TEST_TICKET_PRICE_DISCRIMINATOR,
  );
}

export type SetTestTicketPriceInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountRaffleState extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRaffleState extends string
        ? WritableAccount<TAccountRaffleState>
        : TAccountRaffleState,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type SetTestTicketPriceInstructionData = {
  discriminator: ReadonlyUint8Array;
  price: Option<bigint>;
};

export type SetTestTicketPriceInstructionDataArgs = {
  price: OptionOrNullable<number | bigint>;
};

export function getSetTestTicketPriceInstructionDataEncoder(): Encoder<SetTestTicketPriceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["price", getOptionEncoder(getU64Encoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_TEST_TICKET_PRICE_DISCRIMINATOR,
    }),
  );
}

export function getSetTestTicketPriceInstructionDataDecoder(): Decoder<SetTestTicketPriceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["price", getOptionDecoder(getU64Decoder())],
  ]);
}

export function getSetTestTicketPriceInstructionDataCodec(): Codec<
  SetTestTicketPriceInstructionDataArgs,
  SetTestTicketPriceInstructionData
> {
  return combineCodec(
    getSetTestTicketPriceInstructionDataEncoder(),
    getSetTestTicketPriceInstructionDataDecoder(),
  );
}

export type SetTestTicketPriceAsyncInput<
  TAccountRaffleState extends string = string,
  TAccountAuthority extends string = string,
> = {
  raffleState?: Address<TAccountRaffleState>;
  authority: TransactionSigner<TAccountAuthority>;
  price: SetTestTicketPriceInstructionDataArgs["price"];
};

export async function getSetTestTicketPriceInstructionAsync<
  TAccountRaffleState extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: SetTestTicketPriceAsyncInput<TAccountRaffleState, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetTestTicketPriceInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.raffleState.value) {
    accounts.raffleState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 97, 102, 102, 108, 101, 95, 115, 116, 97, 116, 101,
          ]),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.raffleState),
      getAccountMeta(accounts.authority),
    ],
    data: getSetTestTicketPriceInstructionDataEncoder().encode(
      args as SetTestTicketPriceInstructionDataArgs,
    ),
    programAddress,
  } as SetTestTicketPriceInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountAuthority
  >);
}

export type SetTestTicketPriceInput<
  TAccountRaffleState extends string = string,
  TAccountAuthority extends string = string,
> = {
  raffleState: Address<TAccountRaffleState>;
  authority: TransactionSigner<TAccountAuthority>;
  price: SetTestTicketPriceInstructionDataArgs["price"];
};

export function getSetTestTicketPriceInstruction<
  TAccountRaffleState extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: SetTestTicketPriceInput<TAccountRaffleState, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): SetTestTicketPriceInstruction<
  TProgramAddress,
  TAccountRaffleState,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.raffleState),
      getAccountMeta(accounts.authority),
    ],
    data: getSetTestTicketPriceInstructionDataEncoder().encode(
      args as SetTestTicketPriceInstructionDataArgs,
    ),
    programAddress,
  } as SetTestTicketPriceInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountAuthority
  >);
}

export type ParsedSetTestTicketPriceInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    raffleState: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: SetTestTicketPriceInstructionData;
};

export function parseSetTestTicketPriceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetTestTicketPriceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { raffleState: getNextAccount(), authority: getNextAccount() },
    data: getSetTestTicketPriceInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
