/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  combineCodec,
  fixDecoderSize,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  transformEncoder,
  type WritableAccount,
} from "gill";
import { u32ToLittleEndianBuffer } from "utils/buffer";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { expectAddress, expectSome, getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_WINNER_ADDRESS_DISCRIMINATOR = new Uint8Array([155, 18, 56, 22, 22, 1, 188, 125]);

export function getSetWinnerAddressDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_WINNER_ADDRESS_DISCRIMINATOR);
}

export type SetWinnerAddressInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountRaffleState extends string | AccountMeta<string> = string,
  TAccountSolRaffle extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountRoundTicketsPurchase extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRaffleState extends string ? WritableAccount<TAccountRaffleState> : TAccountRaffleState,
      TAccountSolRaffle extends string ? WritableAccount<TAccountSolRaffle> : TAccountSolRaffle,
      TAccountRound extends string ? WritableAccount<TAccountRound> : TAccountRound,
      TAccountRoundTicketsPurchase extends string
        ? ReadonlyAccount<TAccountRoundTicketsPurchase>
        : TAccountRoundTicketsPurchase,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type SetWinnerAddressInstructionData = {
  discriminator: ReadonlyUint8Array;
  roundId: number;
  purchaseIndex: number;
};

export type SetWinnerAddressInstructionDataArgs = {
  roundId: number;
  purchaseIndex: number;
};

export function getSetWinnerAddressInstructionDataEncoder(): FixedSizeEncoder<SetWinnerAddressInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["roundId", getU32Encoder()],
      ["purchaseIndex", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_WINNER_ADDRESS_DISCRIMINATOR }),
  );
}

export function getSetWinnerAddressInstructionDataDecoder(): FixedSizeDecoder<SetWinnerAddressInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["roundId", getU32Decoder()],
    ["purchaseIndex", getU32Decoder()],
  ]);
}

export function getSetWinnerAddressInstructionDataCodec(): FixedSizeCodec<
  SetWinnerAddressInstructionDataArgs,
  SetWinnerAddressInstructionData
> {
  return combineCodec(getSetWinnerAddressInstructionDataEncoder(), getSetWinnerAddressInstructionDataDecoder());
}

export type SetWinnerAddressAsyncInput<
  TAccountRaffleState extends string = string,
  TAccountSolRaffle extends string = string,
  TAccountRound extends string = string,
  TAccountRoundTicketsPurchase extends string = string,
  TAccountAuthority extends string = string,
> = {
  raffleState?: Address<TAccountRaffleState>;
  solRaffle?: Address<TAccountSolRaffle>;
  round?: Address<TAccountRound>;
  roundTicketsPurchase?: Address<TAccountRoundTicketsPurchase>;
  authority: TransactionSigner<TAccountAuthority>;
  roundId: SetWinnerAddressInstructionDataArgs["roundId"];
  purchaseIndex: SetWinnerAddressInstructionDataArgs["purchaseIndex"];
};

export async function getSetWinnerAddressInstructionAsync<
  TAccountRaffleState extends string,
  TAccountSolRaffle extends string,
  TAccountRound extends string,
  TAccountRoundTicketsPurchase extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: SetWinnerAddressAsyncInput<
    TAccountRaffleState,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTicketsPurchase,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetWinnerAddressInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTicketsPurchase,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: true },
    solRaffle: { value: input.solRaffle ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    roundTicketsPurchase: {
      value: input.roundTicketsPurchase ?? null,
      isWritable: false,
    },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.raffleState.value) {
    accounts.raffleState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([114, 97, 102, 102, 108, 101, 95, 115, 116, 97, 116, 101]))],
    });
  }
  if (!accounts.solRaffle.value) {
    accounts.solRaffle.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getBytesEncoder().encode(new Uint8Array([115, 111, 108, 95, 114, 97, 102, 102, 108, 101]))],
    });
  }
  if (!accounts.round.value) {
    accounts.round.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([114, 111, 117, 110, 100])),
        getAddressEncoder().encode(expectAddress(accounts.solRaffle.value)),
        u32ToLittleEndianBuffer(args.roundId),
      ],
    });
  }
  if (!accounts.roundTicketsPurchase.value) {
    accounts.roundTicketsPurchase.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 111, 117, 110, 100, 95, 116, 105, 99, 107, 101, 116, 115, 95, 112, 117, 114, 99, 104, 97, 115, 101,
          ]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.round.value)),
        getU32Encoder().encode(expectSome(args.purchaseIndex)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.raffleState),
      getAccountMeta(accounts.solRaffle),
      getAccountMeta(accounts.round),
      getAccountMeta(accounts.roundTicketsPurchase),
      getAccountMeta(accounts.authority),
    ],
    data: getSetWinnerAddressInstructionDataEncoder().encode(args as SetWinnerAddressInstructionDataArgs),
    programAddress,
  } as SetWinnerAddressInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTicketsPurchase,
    TAccountAuthority
  >);
}

export type SetWinnerAddressInput<
  TAccountRaffleState extends string = string,
  TAccountSolRaffle extends string = string,
  TAccountRound extends string = string,
  TAccountRoundTicketsPurchase extends string = string,
  TAccountAuthority extends string = string,
> = {
  raffleState: Address<TAccountRaffleState>;
  solRaffle: Address<TAccountSolRaffle>;
  round: Address<TAccountRound>;
  roundTicketsPurchase: Address<TAccountRoundTicketsPurchase>;
  authority: TransactionSigner<TAccountAuthority>;
  roundId: SetWinnerAddressInstructionDataArgs["roundId"];
  purchaseIndex: SetWinnerAddressInstructionDataArgs["purchaseIndex"];
};

export function getSetWinnerAddressInstruction<
  TAccountRaffleState extends string,
  TAccountSolRaffle extends string,
  TAccountRound extends string,
  TAccountRoundTicketsPurchase extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: SetWinnerAddressInput<
    TAccountRaffleState,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTicketsPurchase,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): SetWinnerAddressInstruction<
  TProgramAddress,
  TAccountRaffleState,
  TAccountSolRaffle,
  TAccountRound,
  TAccountRoundTicketsPurchase,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: true },
    solRaffle: { value: input.solRaffle ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    roundTicketsPurchase: {
      value: input.roundTicketsPurchase ?? null,
      isWritable: false,
    },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.raffleState),
      getAccountMeta(accounts.solRaffle),
      getAccountMeta(accounts.round),
      getAccountMeta(accounts.roundTicketsPurchase),
      getAccountMeta(accounts.authority),
    ],
    data: getSetWinnerAddressInstructionDataEncoder().encode(args as SetWinnerAddressInstructionDataArgs),
    programAddress,
  } as SetWinnerAddressInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountSolRaffle,
    TAccountRound,
    TAccountRoundTicketsPurchase,
    TAccountAuthority
  >);
}

export type ParsedSetWinnerAddressInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    raffleState: TAccountMetas[0];
    solRaffle: TAccountMetas[1];
    round: TAccountMetas[2];
    roundTicketsPurchase: TAccountMetas[3];
    authority: TAccountMetas[4];
  };
  data: SetWinnerAddressInstructionData;
};

export function parseSetWinnerAddressInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedSetWinnerAddressInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      raffleState: getNextAccount(),
      solRaffle: getNextAccount(),
      round: getNextAccount(),
      roundTicketsPurchase: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getSetWinnerAddressInstructionDataDecoder().decode(instruction.data),
  };
}
