/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "gill";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const TEST_CALCULATE_PRICE_DISCRIMINATOR = new Uint8Array([
  42, 160, 154, 127, 134, 251, 71, 95,
]);

export function getTestCalculatePriceDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TEST_CALCULATE_PRICE_DISCRIMINATOR,
  );
}

export type TestCalculatePriceInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountBtcPriceFeed extends string | AccountMeta<string> = string,
  TAccountSolPriceFeed extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBtcPriceFeed extends string
        ? ReadonlyAccount<TAccountBtcPriceFeed>
        : TAccountBtcPriceFeed,
      TAccountSolPriceFeed extends string
        ? ReadonlyAccount<TAccountSolPriceFeed>
        : TAccountSolPriceFeed,
      ...TRemainingAccounts,
    ]
  >;

export type TestCalculatePriceInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type TestCalculatePriceInstructionDataArgs = {};

export function getTestCalculatePriceInstructionDataEncoder(): FixedSizeEncoder<TestCalculatePriceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: TEST_CALCULATE_PRICE_DISCRIMINATOR,
    }),
  );
}

export function getTestCalculatePriceInstructionDataDecoder(): FixedSizeDecoder<TestCalculatePriceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getTestCalculatePriceInstructionDataCodec(): FixedSizeCodec<
  TestCalculatePriceInstructionDataArgs,
  TestCalculatePriceInstructionData
> {
  return combineCodec(
    getTestCalculatePriceInstructionDataEncoder(),
    getTestCalculatePriceInstructionDataDecoder(),
  );
}

export type TestCalculatePriceInput<
  TAccountBtcPriceFeed extends string = string,
  TAccountSolPriceFeed extends string = string,
> = {
  btcPriceFeed: Address<TAccountBtcPriceFeed>;
  solPriceFeed: Address<TAccountSolPriceFeed>;
};

export function getTestCalculatePriceInstruction<
  TAccountBtcPriceFeed extends string,
  TAccountSolPriceFeed extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: TestCalculatePriceInput<TAccountBtcPriceFeed, TAccountSolPriceFeed>,
  config?: { programAddress?: TProgramAddress },
): TestCalculatePriceInstruction<
  TProgramAddress,
  TAccountBtcPriceFeed,
  TAccountSolPriceFeed
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    btcPriceFeed: { value: input.btcPriceFeed ?? null, isWritable: false },
    solPriceFeed: { value: input.solPriceFeed ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.btcPriceFeed),
      getAccountMeta(accounts.solPriceFeed),
    ],
    data: getTestCalculatePriceInstructionDataEncoder().encode({}),
    programAddress,
  } as TestCalculatePriceInstruction<
    TProgramAddress,
    TAccountBtcPriceFeed,
    TAccountSolPriceFeed
  >);
}

export type ParsedTestCalculatePriceInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    btcPriceFeed: TAccountMetas[0];
    solPriceFeed: TAccountMetas[1];
  };
  data: TestCalculatePriceInstructionData;
};

export function parseTestCalculatePriceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedTestCalculatePriceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      btcPriceFeed: getNextAccount(),
      solPriceFeed: getNextAccount(),
    },
    data: getTestCalculatePriceInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
