/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "gill";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const WITHDRAW_RENT_VAULT_DISCRIMINATOR = new Uint8Array([
  102, 130, 133, 24, 194, 221, 219, 45,
]);

export function getWithdrawRentVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WITHDRAW_RENT_VAULT_DISCRIMINATOR,
  );
}

export type WithdrawRentVaultInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountRaffleState extends string | AccountMeta<string> = string,
  TAccountRentVault extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRaffleState extends string
        ? WritableAccount<TAccountRaffleState>
        : TAccountRaffleState,
      TAccountRentVault extends string
        ? WritableAccount<TAccountRentVault>
        : TAccountRentVault,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type WithdrawRentVaultInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type WithdrawRentVaultInstructionDataArgs = { amount: number | bigint };

export function getWithdrawRentVaultInstructionDataEncoder(): FixedSizeEncoder<WithdrawRentVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: WITHDRAW_RENT_VAULT_DISCRIMINATOR }),
  );
}

export function getWithdrawRentVaultInstructionDataDecoder(): FixedSizeDecoder<WithdrawRentVaultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getWithdrawRentVaultInstructionDataCodec(): FixedSizeCodec<
  WithdrawRentVaultInstructionDataArgs,
  WithdrawRentVaultInstructionData
> {
  return combineCodec(
    getWithdrawRentVaultInstructionDataEncoder(),
    getWithdrawRentVaultInstructionDataDecoder(),
  );
}

export type WithdrawRentVaultAsyncInput<
  TAccountRaffleState extends string = string,
  TAccountRentVault extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  raffleState?: Address<TAccountRaffleState>;
  rentVault?: Address<TAccountRentVault>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: WithdrawRentVaultInstructionDataArgs["amount"];
};

export async function getWithdrawRentVaultInstructionAsync<
  TAccountRaffleState extends string,
  TAccountRentVault extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: WithdrawRentVaultAsyncInput<
    TAccountRaffleState,
    TAccountRentVault,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  WithdrawRentVaultInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountRentVault,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: true },
    rentVault: { value: input.rentVault ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.raffleState.value) {
    accounts.raffleState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 97, 102, 102, 108, 101, 95, 115, 116, 97, 116, 101,
          ]),
        ),
      ],
    });
  }
  if (!accounts.rentVault.value) {
    accounts.rentVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([114, 101, 110, 116, 95, 118, 97, 117, 108, 116]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.raffleState),
      getAccountMeta(accounts.rentVault),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getWithdrawRentVaultInstructionDataEncoder().encode(
      args as WithdrawRentVaultInstructionDataArgs,
    ),
    programAddress,
  } as WithdrawRentVaultInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountRentVault,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type WithdrawRentVaultInput<
  TAccountRaffleState extends string = string,
  TAccountRentVault extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  raffleState: Address<TAccountRaffleState>;
  rentVault: Address<TAccountRentVault>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: WithdrawRentVaultInstructionDataArgs["amount"];
};

export function getWithdrawRentVaultInstruction<
  TAccountRaffleState extends string,
  TAccountRentVault extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: WithdrawRentVaultInput<
    TAccountRaffleState,
    TAccountRentVault,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): WithdrawRentVaultInstruction<
  TProgramAddress,
  TAccountRaffleState,
  TAccountRentVault,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: true },
    rentVault: { value: input.rentVault ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.raffleState),
      getAccountMeta(accounts.rentVault),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getWithdrawRentVaultInstructionDataEncoder().encode(
      args as WithdrawRentVaultInstructionDataArgs,
    ),
    programAddress,
  } as WithdrawRentVaultInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountRentVault,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedWithdrawRentVaultInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    raffleState: TAccountMetas[0];
    rentVault: TAccountMetas[1];
    authority: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: WithdrawRentVaultInstructionData;
};

export function parseWithdrawRentVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedWithdrawRentVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      raffleState: getNextAccount(),
      rentVault: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getWithdrawRentVaultInstructionDataDecoder().decode(instruction.data),
  };
}
