/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "gill";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const WITHDRAW_VRF_VAULT_DISCRIMINATOR = new Uint8Array([
  14, 31, 4, 129, 171, 151, 32, 83,
]);

export function getWithdrawVrfVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WITHDRAW_VRF_VAULT_DISCRIMINATOR,
  );
}

export type WithdrawVrfVaultInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountRaffleState extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountVrfFeeVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRaffleState extends string
        ? WritableAccount<TAccountRaffleState>
        : TAccountRaffleState,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountVrfFeeVault extends string
        ? WritableAccount<TAccountVrfFeeVault>
        : TAccountVrfFeeVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type WithdrawVrfVaultInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type WithdrawVrfVaultInstructionDataArgs = { amount: number | bigint };

export function getWithdrawVrfVaultInstructionDataEncoder(): FixedSizeEncoder<WithdrawVrfVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: WITHDRAW_VRF_VAULT_DISCRIMINATOR }),
  );
}

export function getWithdrawVrfVaultInstructionDataDecoder(): FixedSizeDecoder<WithdrawVrfVaultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getWithdrawVrfVaultInstructionDataCodec(): FixedSizeCodec<
  WithdrawVrfVaultInstructionDataArgs,
  WithdrawVrfVaultInstructionData
> {
  return combineCodec(
    getWithdrawVrfVaultInstructionDataEncoder(),
    getWithdrawVrfVaultInstructionDataDecoder(),
  );
}

export type WithdrawVrfVaultAsyncInput<
  TAccountRaffleState extends string = string,
  TAccountAuthority extends string = string,
  TAccountVrfFeeVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  raffleState?: Address<TAccountRaffleState>;
  authority: TransactionSigner<TAccountAuthority>;
  vrfFeeVault?: Address<TAccountVrfFeeVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: WithdrawVrfVaultInstructionDataArgs["amount"];
};

export async function getWithdrawVrfVaultInstructionAsync<
  TAccountRaffleState extends string,
  TAccountAuthority extends string,
  TAccountVrfFeeVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: WithdrawVrfVaultAsyncInput<
    TAccountRaffleState,
    TAccountAuthority,
    TAccountVrfFeeVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  WithdrawVrfVaultInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountAuthority,
    TAccountVrfFeeVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    vrfFeeVault: { value: input.vrfFeeVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.raffleState.value) {
    accounts.raffleState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 97, 102, 102, 108, 101, 95, 115, 116, 97, 116, 101,
          ]),
        ),
      ],
    });
  }
  if (!accounts.vrfFeeVault.value) {
    accounts.vrfFeeVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            118, 114, 102, 95, 102, 101, 101, 95, 118, 97, 117, 108, 116,
          ]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.raffleState),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.vrfFeeVault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getWithdrawVrfVaultInstructionDataEncoder().encode(
      args as WithdrawVrfVaultInstructionDataArgs,
    ),
    programAddress,
  } as WithdrawVrfVaultInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountAuthority,
    TAccountVrfFeeVault,
    TAccountSystemProgram
  >);
}

export type WithdrawVrfVaultInput<
  TAccountRaffleState extends string = string,
  TAccountAuthority extends string = string,
  TAccountVrfFeeVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  raffleState: Address<TAccountRaffleState>;
  authority: TransactionSigner<TAccountAuthority>;
  vrfFeeVault: Address<TAccountVrfFeeVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: WithdrawVrfVaultInstructionDataArgs["amount"];
};

export function getWithdrawVrfVaultInstruction<
  TAccountRaffleState extends string,
  TAccountAuthority extends string,
  TAccountVrfFeeVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: WithdrawVrfVaultInput<
    TAccountRaffleState,
    TAccountAuthority,
    TAccountVrfFeeVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): WithdrawVrfVaultInstruction<
  TProgramAddress,
  TAccountRaffleState,
  TAccountAuthority,
  TAccountVrfFeeVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    raffleState: { value: input.raffleState ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    vrfFeeVault: { value: input.vrfFeeVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.raffleState),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.vrfFeeVault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getWithdrawVrfVaultInstructionDataEncoder().encode(
      args as WithdrawVrfVaultInstructionDataArgs,
    ),
    programAddress,
  } as WithdrawVrfVaultInstruction<
    TProgramAddress,
    TAccountRaffleState,
    TAccountAuthority,
    TAccountVrfFeeVault,
    TAccountSystemProgram
  >);
}

export type ParsedWithdrawVrfVaultInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    raffleState: TAccountMetas[0];
    authority: TAccountMetas[1];
    vrfFeeVault: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: WithdrawVrfVaultInstructionData;
};

export function parseWithdrawVrfVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedWithdrawVrfVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      raffleState: getNextAccount(),
      authority: getNextAccount(),
      vrfFeeVault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getWithdrawVrfVaultInstructionDataDecoder().decode(instruction.data),
  };
}
