/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  type Codec,
  type Decoder,
  type Encoder,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "gill";
import {
  getResponseDecoder,
  getResponseEncoder,
  type Response,
  type ResponseArgs,
} from ".";

/** Fulfilled request representation. */
export type Fulfilled = {
  /**
   * Generated randomness.
   *
   * It is validated within the fulfill instruction. Please look into the account history logs
   * and VRF events to observe the individual components.
   */
  randomness: ReadonlyUint8Array;
  /**
   * Individual responses constituting the randomness.
   *
   * This going to be `Some` within the callback invocation, and `None` afterwards.
   */
  responses: Option<Array<Response>>;
};

export type FulfilledArgs = {
  /**
   * Generated randomness.
   *
   * It is validated within the fulfill instruction. Please look into the account history logs
   * and VRF events to observe the individual components.
   */
  randomness: ReadonlyUint8Array;
  /**
   * Individual responses constituting the randomness.
   *
   * This going to be `Some` within the callback invocation, and `None` afterwards.
   */
  responses: OptionOrNullable<Array<ResponseArgs>>;
};

export function getFulfilledEncoder(): Encoder<FulfilledArgs> {
  return getStructEncoder([
    ["randomness", fixEncoderSize(getBytesEncoder(), 64)],
    ["responses", getOptionEncoder(getArrayEncoder(getResponseEncoder()))],
  ]);
}

export function getFulfilledDecoder(): Decoder<Fulfilled> {
  return getStructDecoder([
    ["randomness", fixDecoderSize(getBytesDecoder(), 64)],
    ["responses", getOptionDecoder(getArrayDecoder(getResponseDecoder()))],
  ]);
}

export function getFulfilledCodec(): Codec<FulfilledArgs, Fulfilled> {
  return combineCodec(getFulfilledEncoder(), getFulfilledDecoder());
}
