/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
} from "gill";

/** Oracle configuration */
export type NetworkConfiguration = {
  /** An authority. */
  authority: Address;
  /** Treasury account address. */
  treasury: Address;
  /** Per-request fee paid by a client. */
  requestFee: bigint;
  /**
   * Callback deadline measured in slots (1 slot is approximately 400ms).
   *
   * If callback keeps failing util this deadline reached, then the request
   * will be fulfilled without calling the callback.
   *
   * Note that well-written callback should never fail, so this should never
   * apply to your client.
   */
  callbackDeadline: bigint;
  /** This parties are authorized to fulfill requests. */
  fulfillAuthorities: Array<Address>;
};

export type NetworkConfigurationArgs = {
  /** An authority. */
  authority: Address;
  /** Treasury account address. */
  treasury: Address;
  /** Per-request fee paid by a client. */
  requestFee: number | bigint;
  /**
   * Callback deadline measured in slots (1 slot is approximately 400ms).
   *
   * If callback keeps failing util this deadline reached, then the request
   * will be fulfilled without calling the callback.
   *
   * Note that well-written callback should never fail, so this should never
   * apply to your client.
   */
  callbackDeadline: number | bigint;
  /** This parties are authorized to fulfill requests. */
  fulfillAuthorities: Array<Address>;
};

export function getNetworkConfigurationEncoder(): Encoder<NetworkConfigurationArgs> {
  return getStructEncoder([
    ["authority", getAddressEncoder()],
    ["treasury", getAddressEncoder()],
    ["requestFee", getU64Encoder()],
    ["callbackDeadline", getU64Encoder()],
    ["fulfillAuthorities", getArrayEncoder(getAddressEncoder())],
  ]);
}

export function getNetworkConfigurationDecoder(): Decoder<NetworkConfiguration> {
  return getStructDecoder([
    ["authority", getAddressDecoder()],
    ["treasury", getAddressDecoder()],
    ["requestFee", getU64Decoder()],
    ["callbackDeadline", getU64Decoder()],
    ["fulfillAuthorities", getArrayDecoder(getAddressDecoder())],
  ]);
}

export function getNetworkConfigurationCodec(): Codec<
  NetworkConfigurationArgs,
  NetworkConfiguration
> {
  return combineCodec(
    getNetworkConfigurationEncoder(),
    getNetworkConfigurationDecoder(),
  );
}
