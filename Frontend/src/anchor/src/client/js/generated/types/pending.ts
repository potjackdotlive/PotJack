/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  type Codec,
  type Decoder,
  type Encoder,
  type Option,
  type OptionOrNullable,
} from "gill";
import {
  getResponseDecoder,
  getResponseEncoder,
  getValidatedCallbackDecoder,
  getValidatedCallbackEncoder,
  type Response,
  type ResponseArgs,
  type ValidatedCallback,
  type ValidatedCallbackArgs,
} from ".";

/** Pending randomness request. */
export type Pending = {
  /** Responses collected so far. */
  responses: Array<Response>;
  /** Callback (if any). */
  callback: Option<ValidatedCallback>;
  /** If `true` then [`Pending::callback`] is a request-level callback. */
  callbackOverride: boolean;
};

export type PendingArgs = {
  /** Responses collected so far. */
  responses: Array<ResponseArgs>;
  /** Callback (if any). */
  callback: OptionOrNullable<ValidatedCallbackArgs>;
  /** If `true` then [`Pending::callback`] is a request-level callback. */
  callbackOverride: boolean;
};

export function getPendingEncoder(): Encoder<PendingArgs> {
  return getStructEncoder([
    ["responses", getArrayEncoder(getResponseEncoder())],
    ["callback", getOptionEncoder(getValidatedCallbackEncoder())],
    ["callbackOverride", getBooleanEncoder()],
  ]);
}

export function getPendingDecoder(): Decoder<Pending> {
  return getStructDecoder([
    ["responses", getArrayDecoder(getResponseDecoder())],
    ["callback", getOptionDecoder(getValidatedCallbackDecoder())],
    ["callbackOverride", getBooleanDecoder()],
  ]);
}

export function getPendingCodec(): Codec<PendingArgs, Pending> {
  return combineCodec(getPendingEncoder(), getPendingDecoder());
}
