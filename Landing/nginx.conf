server {
    listen 80;
    server_name _;

    # Handle client-side routing (SPA)
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
